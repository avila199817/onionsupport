/* =========================================================
   🔥 MINI TOPBAR — GENERIC SAAS COMPONENT
========================================================= */

/* =========================
   WRAPPER
========================= */
.mini-topbar{
  position: fixed;

  top: var(--topbar-height);
  left: var(--sidebar-width);
  right: 0;

  z-index: var(--z-topbar, 100);

  display:flex;
  flex-direction:column;

  background:var(--panel-bg);
  border-bottom:1px solid var(--panel-border);

  isolation:isolate;

  transition:
    left var(--duration-slow) var(--ease-out),
    background var(--duration-normal) ease;
}

/* SIDEBAR COLLAPSED */
.sidebar.collapsed ~ .main-content .mini-topbar{
  left: var(--sidebar-collapsed);
}

/* =========================
   INNER LAYOUT
========================= */
.mini-topbar-inner{
  display:flex;
  align-items:center;
  gap:var(--space-md);

  padding:12px 16px;
}

/* =========================
   FLEX AREAS
========================= */
.mini-topbar-left{
  display:flex;
  align-items:center;
  gap:var(--space-md);
  min-width:0;
}

.mini-topbar-center{
  flex:1;
  display:flex;
  align-items:center;
  justify-content:center;
}

.mini-topbar-right{
  display:flex;
  align-items:center;
  gap:var(--space-md);
  margin-left:auto;
}

/* =========================
   INPUTS / CONTROLS
========================= */
.mini-topbar input,
.mini-topbar select{
  padding:10px 12px;
  border-radius:8px;

  border:1px solid var(--card-border);

  background:var(--card-bg);
  color:var(--text);

  font-size:13px;

  transition:
    border-color var(--duration-normal) ease,
    background var(--duration-normal) ease;
}

.mini-topbar input:focus,
.mini-topbar select:focus{
  border-color:var(--accent);
  background:var(--panel-bg);
}

/* =========================
   BUTTON PRIMARY
========================= */
.mini-topbar .btn-primary{
  padding:10px 18px;
  border-radius:999px;

  background:linear-gradient(135deg,var(--accent),var(--accent-hover));
  color:#fff;

  font-weight:600;
  border:none;
  cursor:pointer;

  box-shadow:0 6px 18px rgba(99,102,241,0.35);

  transition:all .2s ease;
}

.mini-topbar .btn-primary:hover{
  transform:translateY(-1px);
  box-shadow:0 10px 26px rgba(99,102,241,0.45);
}

/* =========================
   TABLE HEADER (OPTIONAL)
========================= */
.mini-topbar-table{
  border-top:1px solid var(--panel-border);
}

.mini-topbar-table table{
  width:100%;
  table-layout:fixed;

  border-collapse:separate;
  border-spacing:0;
}

.mini-topbar-table th{
  padding:12px 14px;

  text-align:left;
  font-size:11px;

  color:var(--dim);
  text-transform:uppercase;
  letter-spacing:.4px;

  white-space:nowrap;
}

/* =========================
   PAGE OFFSET (IMPORTANTE)
========================= */
.has-mini-topbar{
  padding-top:120px; /* ajustar según altura real */
}
