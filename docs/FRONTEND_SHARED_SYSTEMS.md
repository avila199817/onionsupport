# Sistemas compartidos del frontend

> Consolidación implementada en [PR #487](https://github.com/avila199817/onionsupport/pull/487), con corrección de consentimiento en [PR #490](https://github.com/avila199817/onionsupport/pull/490). La [entrega del 2026-09-08](releases/2026-09-08-single-modal-session.md) amplía la autoridad de modales a una sola sesión de detalle para los cuatro dominios. Estado de release, métricas y límites: [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md). Próximas entregas: [ROADMAP.md](ROADMAP.md).

Una autoridad por responsabilidad permite corregir un comportamiento en un solo lugar. No exige reunir toda la aplicación en un archivo: las vistas conservan su contenido y sus reglas de negocio, mientras delegan las operaciones comunes.

| Responsabilidad | Autoridad | Integración |
| --- | --- | --- |
| Identidad, iniciales, color y estado de imagen | `src/features/avatar-system/index.js` | Los consumidores describen la identidad; no gestionan otro fallback de imagen. |
| Apertura de detalle, origen y sustitución de entidad | `src/features/entity-overlay/index.js` | Home, listas, relaciones y APIs públicas invocan `EntityOverlay.open`; una sesión y un controlador de detalle. |
| Shell modal: estructura DOM, ARIA, tamaños, cierre, estados y CSS estructural | `src/features/entity-overlay/modal-host.js` (`renderModalShell`) y `src/css/components/detail-modal.css` | Un solo shell (`ui-modal-shell.v1`) para todo diálogo privado; los dominios aportan contenido, acciones e identidad `data-*`. Contrato: `tools/modal-shell-contract.mjs`. |
| Escape, Tab, click exterior, pila, foco y scroll modal | `src/features/entity-overlay/modal-lifecycle.js` | Cada propietario conserva render, borradores, confirmación y política de cierre; recibe Escape y click exterior como peticiones. |
| Montaje, retirada y actualización del panel privado | `src/features/entity-overlay/modal-host.js` | Una lease conserva la propiedad del nodo; el renderer mantiene panel, foco y scroll al actualizar el mismo diálogo. |
| Perfil confirmado de la sesión actual | `src/features/user-profile/index.js` y Core | Cuenta y Usuarios comparten reconciliación y orden de escrituras. Core conserva el único usuario de sesión. |
| Métricas y estadísticas de Facturas | `src/core/statistics.js`, `src/views/facturas/facturas.stats.js` | Selectores puros, sin caché adicional. Home y Facturas comparten los campos del servidor y distinguen desconocido de cero. Si el backend acota su recorrido (`stats.truncated === true`, `documentsLimit`), Home añade el aviso `FACTURAS_STATS_TRUNCATED` al dominio `facturas_stats` (panel "Parcial") y Facturas etiqueta las tarjetas de recuento e importe como parciales; los totales calculados se muestran, no se sustituyen ni se inventan. |
| Normalización de texto en una línea (`cleanText`) | `src/core/presentation-text.js` | Una sola definición bajo cualquier nombre: kernel (`core`, `config`, `media`, `http`, `app`, `loader`, `router`), `auth`, vistas públicas, features (incluido el sistema de avatares, que ya no exporta `cleanAvatarText`) y vistas privadas la importan (o la reexportan en vivo, como Home y la vista pendiente); las variantes de dominio la componen (`clipText` en topbar-executive, `cleanMessageText` en WhatsApp, `text` sólo-cadenas en user-identity, `safePageTitle` en analytics, `filename` y la clave de prioridad en Incidencias). Tres políticas distintas conservan su cuerpo y están listadas en el contrato: `redact` en `main.js`, el título de ruta en `public-site.js` y `attrExact` en Clientes. El módulo sólo contiene el normalizador para que los cierres de arranque no carguen más. Contrato: `tools/presentation-text-contract.mjs` (huella del cuerpo). |
| Clave compacta de comparación (`normalizeKey`) y clave de código (`codeKey`) | `src/core/presentation-text.js` | `normalizeKey` quita separadores (`-`, `_`, espacios) y pasa a minúsculas conservando acentos: es la clave con la que el kernel compara cabeceras, rutas y nombres de configuración (`core`, `http`, `router`, `media`, shared público). `codeKey` une separadores con `_` y pasa a mayúsculas: la forma de los códigos de error y estado del backend (`not-found` → `NOT_FOUND`); `http`, la activación de cuenta y el soporte público (que la compone sobre sus candidatos de código) la importan. Viven junto a `cleanText` porque ya están en los tres cierres de arranque. Contrato: `tools/presentation-text-contract.mjs`. |
| Claves slug de valores de dominio (`slugKey`, `recordKey`, `labelKey`) | `src/core/slug-key.js` | Tres claves con nombre, una por semántica. `slugKey`: minúsculas, sin acentos (NFD), espacios y guiones unidos con `_`, fuera todo salvo `\w`, `:` y `.`; la clave de estados, prioridades, roles y planes en las vistas (Usuarios cursor e Incidencias opciones la importan desde esta unidad; sus copias eran idénticas). `recordKey`: la clave de registro de la API y el modal de Facturas, donde `.` también separa, cualquier otro símbolo se convierte en `_` y `:` se conserva (`meta:foo.bar` → `meta:foo_bar`); tres copias idénticas la importan. `labelKey`: la clave de etiquetas de las plantillas de Facturas, donde todo lo que no es `\w` pasa a `_` (`a.b:c` → `a_b_c`); dos copias idénticas la importan. Módulo propio, fuera de los cierres de arranque. Seis políticas de clave siguen locales con nombre propio y están listadas en el contrato: `handleKey` del perfil técnico (conserva `@`), `homeLabelKey` de la base de Home (símbolos a `_`, 80 caracteres), `normalizeStateKey` de la API de Incidencias, la clave de prioridad, `key` del modal de Incidencias (conservan puntuación) y `technicalVersion` de Facturas (alimenta un `includes`). Ningún módulo se llama ya `normalizeKey` fuera de la clave compacta del kernel. Contrato: `tools/presentation-text-contract.mjs`. |
| Escape HTML de nodos de texto y atributos (`escapeHtml`) | `src/core/escape-html.js` | Una sola definición: templates, controladores, el shell modal, Agenda, el shared público (`escapeAttr` la compone para atributos), el template de Incidencias y el panel de valoraciones la importan; nunca recorta ni colapsa contenido. Ningún otro módulo emite `&amp;` por su cuenta salvo `core/public-legal.js`, que el guard de integridad de la Home exige sin `import`. Contrato: `tools/presentation-text-contract.mjs`. |
| Guards de forma: objeto plano y función (`isObject`, `safeObject`, `isFunction`) | `src/core/objects.js` | Una sola definición: kernel (`core`, `http`, `app`, `router`, `auth`), UI compartida, features y vistas la importan (la base de plantillas de Home la reexporta en vivo). Un `fallback` por parámetro sustituye a las copias con `{}` o `null` por defecto. `main.js` conserva sus `isObject` e `isFunction` porque la entrada no puede importar módulos compartidos sin que rolldown los pliegue en su chunk (medido: `auth` de 63835 a 102979 bytes). `http.isPlainObject` la compone con la comprobación de prototipo. Contrato: `tools/shape-guards-contract.mjs`. |
| Guards de forma: arrays (`safeArray`, `arrayFrom`) | `src/core/arrays.js` | Dos políticas reales, dos funciones: `safeArray` sólo acepta arrays; `arrayFrom` acepta también array-likes (`NodeList`, `FileList`, `arguments`) y rechaza cadenas y escalares. Módulo separado de los guards de objeto para que el cierre `auth` no cargue la coerción de arrays. Conservan su política propia `incidencias-media-preview` (iterables) y `toArray` de la Home pública (descarta falsy). Contrato: `tools/shape-guards-contract.mjs`. |
| Primer candidato de una cadena de fallback (`firstNonBlank`, `firstNonEmpty`) | `src/core/objects.js` | Dos políticas con nombre en lugar de 44 copias llamadas `first`, junto a los guards de objeto porque un módulo propio se convierte en un chunk que importan los tres cierres de arranque (medido: +529 B en la Home pública): `firstNonBlank` salta `null`, `undefined` y cadenas en blanco (kernel `core`, `http`, `auth`, `entity-overlay`, soporte público, Clientes, WhatsApp, 12 módulos); `firstNonEmpty` salta además arrays vacíos y objetos planos sin claves (32 módulos: Facturas, Incidencias, Usuarios, Cuenta, Home, Servidor, topbar). Ninguna aplana sus argumentos: un array es un valor (`repo_integrity` mantiene la invariante sobre la autoridad). Los selectores tipados (`firstEmail`, `firstUrl`, `firstObject`, `firstText`, `firstArray`, `firstDefined`) son políticas de dominio y conservan su cuerpo. Contrato: `tools/first-numbers-clock-contract.mjs`. |
| Números (`coercedNumber`, `finiteNumber`, `clamp`) | `src/core/numbers.js` | Una coerción (`Number(value)` finito o el fallback) con dos políticas nombradas para el valor en blanco. `coercedNumber(value = 0, fallback = 0)`: `undefined`, `null`, `""` y espacios son 0, como los lee `Number()` (las copias declaraban `value = 0`); sólo `NaN` e infinitos caen al fallback; contadores, límites y facetas de Clientes, Incidencias y Usuarios (8 copias `number`/`finiteNumber`). `finiteNumber(value, fallback = 0)`: un blanco (`undefined`, `null`, `""`) es el fallback y el resto `Number(value)` si es finito; medidas y campos opcionales de Servidor, Home, el perfil técnico y la factura canónica (`safeNumber` ×3, `number` ×2, `optionalNumber`, `finiteNumber`; las llamadas con fallback `null` lo nombran). Ningún módulo conserva ya el cuerpo con `value = 0`: los tres últimos (vista y API base de Servidor, API de Usuarios) adoptaron la autoridad con sus valores declarados, de modo que una opción omitida aterriza en el valor que la llamada declara y no en 0 (cadencia del modo en vivo 30 s, tope de health 15 s, tiempos de Usuarios 18 s/30 s; antes 5 s, 30 s y 1 s por el suelo del `clamp` o el tope genérico de `core/http`). `tools/declared-timings-contract.mjs` fija esos valores efectivos, el presupuesto del backend que los justifica (15 s por ruta normal, 10 s de Cosmos) y la higiene de abortos y temporizadores; notas `docs/releases/2026-09-16-finite-number-facturas.md` y `docs/releases/2026-09-16-declared-timings.md`. `clamp(value, min, max)` sin política numérica: quien llama analiza antes. Locales con política propia: `correo.api` `clamp(value, fallback, min, max)` (entero con fallback), `integer` de la Home (entero seguro ≥ 0), `nonNegativeInteger` del perfil técnico (compone `finiteNumber`), `parseNumber` de la Home pública (coma decimal), `number` positivo del alta de Incidencias; los parsers de importes tienen su autoridad en la fila de importes (`core/amounts.js`). Contrato: `tools/first-numbers-clock-contract.mjs`. |
| Importes (`parseAmount`, `AMOUNT_POLICIES`, `round2`) | `src/core/amounts.js` | Un mecanismo de texto para importes y cantidades: quita `€ $ £ ¥ %`, conserva dígitos, separadores, signo y espacios (que luego elimina); el último separador decide decimal frente a miles (`1.234,56` y `1,234.56` son 1234,56); sólo comas → coma decimal (`1,5` es 1,5; `1,234` es 1,234); sólo puntos → los lee `Number()`; texto vacío o signo solo → fallback; letras fuera (`1e3` es 13). `undefined` es 0 (las doce copias declaraban `value = 0`; la API de Home nombra su blanco como `null`); `null` y `""` son el fallback; un número es él mismo si es finito. Tres políticas nombradas para lo que no es texto: `coerced` (booleanos y objetos por `Number()`: modelo y modal de Clientes, API y lista de Incidencias), `booleanDigit` (`true`/`false` → 1/0, objetos → fallback: API base, alta y base del modal de Facturas), `textOnly` (booleanos y objetos → fallback: lista, modal y alias de la API de Facturas, API de Home, modal de Incidencias). `round2(value)`: céntimos con `Number.EPSILON` sobre `Number(value)` (alias de la API, modal y vista de Facturas); la API base y el alta de Facturas componen `roundAmount` = `round2(parseAmount(value, 0, booleanDigit))`. Retiradas 12 copias (`number` ×9, `num`, `numberOrNull` ×2; 161 llamadas), 4 `round2` y 2 redondeos inline. `optionalNumber` del modal de Clientes compone la autoridad. Contrato: `tools/amounts-contract.mjs`. |
| Fechas (`toTimestamp`, `toDate`, `TIMESTAMP_POLICIES`) | `src/core/dates.js` | Un parser de valor de payload a milisegundos epoch con tres políticas nombradas para el texto. Mecanismo (lo que hacían las 17 copias, en este orden): `null`/`undefined`/`""` → 0; `Date` → su tiempo (inválido → 0); número finito → segundos epoch hasta 9.999.999.999 (`_ts` de Cosmos, webhook de WhatsApp) y milisegundos por encima; texto numérico igual; otro texto se analiza como fecha; el resto → 0. `toDate` compone un `Date` o `null`. Políticas: `epoch` (texto por `Date.parse`; una fecha sin hora es medianoche UTC; Clientes, Usuarios, Servidor, Home, Incidencias, WhatsApp), `invoiceDay` (una fecha sin hora es medianoche local, la fecha civil de la factura; texto sin `T` toma ese sufijo; modal de Facturas), `invoiceText` (`invoiceDay` más `dd/mm/yyyy[, hh:mm[:ss]]` local; lista de Facturas). Cada llamada nombra su política. Las nueve clases de comportamiento de las copias diferían sólo fuera de lo que envía el backend (texto ISO, números epoch, blancos): umbral 10^10 frente a 10^11, texto numérico aceptado o no, negativos, instancias `Date`, `"0"` como año 2000 de `Date.parse`; la nota de release las lista. Contrato: `tools/dates-contract.mjs`. |
| Formato (`formatCurrency`, `currencyFormatter`, `currencyCode`, `formatDecimal`, `CURRENCY_POLICIES`, `dateFormatter`, `DATE_PRESETS`) | `src/core/format.js` | Primitivas de formato sobre valores ya analizados por el dominio (es-ES). El kernel guarda los formateadores `Intl` (uno por código de divisa y política, construido una vez) y el de número simple; el dominio conserva su parser, su texto de vacío (`"—"`, `0`) y, cuando difiere del estándar (`toFixed(2)` con coma y el código), su texto de respaldo (modal de Clientes, lista y modal de Incidencias componen `currencyFormatter`). Políticas de divisa: `standard` (2 decimales; es-ES agrupa miles desde 5 cifras), `grouped` (miles desde 4 cifras; lista de Incidencias), `precise` (2–4 decimales; costes de Servidor), `currencyDigits` (el mínimo de la divisa, 2 como máximo; modales de Clientes e Incidencias; igual que `standard` para EUR). `formatDecimal` es el número es-ES sin decimales fijos (contadores). Local listada: `axisMoney` de Servidor (notación compacta y decimales según el valor, para el eje de la gráfica). Los importes se analizan en el dominio (unidad de importes). Fechas: `DATE_PRESETS` nombra los juegos de opciones de `Intl.DateTimeFormat` es-ES que muestran varios dominios (`dateTime` 16/09/2026, 04:05; `date`; `shortMonthDate` 16 sept 2026; `shortMonthDateTime`) y `dateFormatter(preset)` guarda un formateador por preset; los envoltorios del dominio conservan su parseo (`toTimestamp` con su política), su texto de vacío y su `catch`. Un preset que muestra un solo módulo sigue en ese módulo (Agenda ×3, Correo ×3, hora de Clientes, h23 de Incidencias ×2, Servidor ×3, WhatsApp), listado como cota en el contrato. Contrato: `tools/format-contract.mjs`. |
| Fuente de tiempo (`nowIso`, `nowMs`) | `src/core/clock.js` | Una sola fuente: cinco copias de `nowIso` (una envuelta en `try/catch` inalcanzable: `new Date()` siempre es válida) y tres `now()` sobre `Date.now()` la importan. `runtime-performance` conserva su `now()` sobre `performance.now()` (reloj monótono, otra semántica). Contrato: `tools/first-numbers-clock-contract.mjs`. |
| Análisis booleano con políticas de tokens (`parseBoolean`, `BOOLEAN_POLICIES`) | `src/core/booleans.js` | Un mecanismo (booleanos pasan; `1`/`"1"` verdadero y `0`/`"0"` falso; una política con `coerce: "strings"` devuelve el fallback para lo que no sea cadena; después la clave slug del valor contra las listas de la política; si no, el fallback, que es el de la propia política cuando la llamada no lo pasa) y seis políticas nombradas que reproducen lo que aceptaba cada consumidor: `switch` (Facturas listado), `switchAny` (base del modal de Facturas), `activity` (altas de Facturas y Usuarios), `activityEs` (modelo de Clientes), `activityEsStrings` (modal de Clientes), `activityEsExtended` (API de Usuarios). Se anidan por tokens (`switch` ⊂ `activity` ⊂ `activityEs` ⊂ `activityEsExtended`): mover un consumidor a una política más amplia es una decisión de producto, no un refactor. Cuatro parsers conservan su política propia y están listados en el contrato: `parseBooleanFlag` (API de Facturas: números distintos de cero verdaderos, `none`/`null` falsos), `normalizeBoolean` (Cuenta: `dark`/`light`, `y`/`n`), `parseStrictBoolean` (API de Usuarios: error de contrato en lugar de fallback) y `bool` de la plantilla de Facturas (cualquier número distinto de 1 es falso). Contrato: `tools/boolean-policies-contract.mjs`. |
| Redacción de secretos (`redactSecrets`, `redactUrl`, `redactTokenPaths`, `SENSITIVE_QUERY_KEYS`) | `src/core/redact.js` | Una autoridad con dos mecanismos nombrados. `redactSecrets(texto)`: pasada por patrones sobre texto libre (mensajes de error, etiquetas, rutas citadas): rutas legacy de reset y activación, asignaciones `nombre=valor` en query o fragmento de los parámetros sensibles, credenciales `Bearer` y tokens con forma de JWT pasan a `***`; conserva los espacios, así que quien presenta compone `cleanText` antes y recorta después (`app`, `auth`, `router`, `sidebar`, `toast`, Facturas, Home, Incidencias, Home pública, login, la vista pendiente, el perfil técnico, topbar ejecutivo y `enhancements`, que limpia después de redactar). `redactUrl(texto)`: variante del kernel para rutas de estado y trazas HTTP (`core`, `http`): analiza el texto como URL, pone a `***` cada parámetro cuya clave compacta es sensible, lo serializa de nuevo (absoluta conserva el origen; el resto vuelve como pathname + search + hash) y aplica la pasada por patrones; un texto que no es URL vuelve como ruta codificada, por lo que sólo se aplica a rutas y URLs (`endpoint`, `url`, `lastUrl`, rutas del snapshot): los mensajes, las cadenas del payload de error y los fragmentos van por `redactSecrets`. `redactTokenPaths`: sólo las rutas de token (`sanitizeLegacyTokenPath` del kernel la compone). Los nombres son `SENSITIVE_QUERY_PARAMS` de `config` (congelados en el contrato: añadir uno cambia lo que el Router elimina de una navegación y lo que `media` acepta); `SENSITIVE_QUERY_KEYS` es su conjunto compacto y lo importan `http`, `core`, `router`, `media` y el shared público, que construían el mismo conjunto por su cuenta. El patrón enmascara además `sas=` (siete copias de vistas lo hacían) sin añadirlo al conjunto de parámetros. `main.js` conserva su copia (la entrada no importa módulos compartidos). Los regex de detección («¿esta URL lleva un secreto?») son otra responsabilidad con listas propias y quedan como cota superior en el contrato. Contrato: `tools/redaction-contract.mjs`. |
| Extracción técnica de un error (`errorMessage`, `ERROR_MESSAGE_POLICIES`, `errorStatus`, `errorCode`, `describeError`) | `src/core/errors.js` | Mensaje: un mecanismo y dos órdenes nombrados. `messageFirst`: el texto propio del `Error` gana sobre el payload del backend (`message`, después `data.message`/`payload.message`, después los campos de `response`); `payloadFirst`: el payload gana (`data.message`/`payload.message`, `response`, después `message`). Para un error HTTP ambos órdenes leen lo mismo, porque `core/http.js` construye `error.message` desde el payload; difieren en un `Error` construido localmente alrededor de un payload (Usuarios y la activación de cuenta adjuntan `data`). El primer candidato no vacío se normaliza a una línea, se redacta (`redactSecrets`) y, si no queda nada, se devuelve el fallback normalizado. Los códigos nunca son un mensaje (los lee la unidad de códigos) y un objeto nunca se convierte en `[object Object]`. Consumidores medidos: `messageFirst` en Clientes (listado y alta), Usuarios (listado y API), Home, Empleados, Facturas, Incidencias, Correo, el overlay de entidades, el perfil técnico y la confirmación de pago; `payloadFirst` en Cuenta (vista y API), Servidor (vista, API base y coste), WhatsApp y el alta de Usuarios. Mover un consumidor de orden es una decisión de producto. Toda llamada nombra su orden como tercer argumento. Tres extractores conservan una política propia y están listados en el contrato: `errorMessage(response)` de la API de Clientes (lee un sobre de respuesta y cae a su `error`/`code`), `requestError` de la API de WhatsApp (elimina caracteres de control, tope 500, construye `WhatsAppApiError`) y `errorMessage(error)` del soporte público (mapa estado → texto del dominio: capa de presentación, no extracción). Estado: `errorStatus(error, fallback)` devuelve el primer candidato (`status`, `statusCode`, los de `response`, los del payload) que es un número finito mayor que cero; `0`, `NaN` y texto no son estado (los `||` de las copias ya los saltaban). Código: `errorCode(error, fallback)` devuelve el primer candidato (`code`, el `error` textual del sobre, los del payload, los de `response`) como clave de código canónica (`codeKey`: separadores a `_`, mayúsculas, la forma que `http` da a todo código); un objeto no es código. Registro: `describeError(error)` es el registro estructurado del kernel (`name`, mensaje a una línea y redactado, estado numérico, código canónico; `null` sin error): `app`, `core` y `auth` (que añade `type`, `canRefresh` y `shouldClearSession`) lo consumen y `enhancements` lo compone con sus topes. Los registros de las API de Facturas, Home e Incidencias componen los tres extractores (mensaje con su fallback, estado, código con su defecto). Consumidores de estado y código: `http` (predicados de autenticación y `lastRefreshError`), `auth`, login, activación y restablecimiento públicos, soporte público, Clientes (vista y API), Correo, Cuenta, Home, Usuarios (API y cursor), WhatsApp y la incorporación de Home; `getErrorStatus` de la API de Usuarios compone `errorStatus` y acota a 100..599 (política propia). Qué mensaje humano muestra un dominio para un código o estado es la capa de presentación. Contrato: `tools/error-extraction-contract.mjs`. |
| Presentación de errores por dominio (`presentError`) | `src/core/error-rules.js` | Capa de presentación sobre la extracción: `presentError(error, rules, fallback)` toma los hechos (`errorStatus`, `errorCode` canónico) y recorre la lista ORDENADA de reglas del dominio; la primera que casa decide. Una regla casa si cualquiera de sus condiciones se cumple: `statuses`, `minStatus`, `offline` (estado 0), `codes` (exactos), `codeIncludes` (subcadenas), `when` (conjunción); `message` y `fallback` son texto o función de los hechos (la activación devuelve `{ field, message, completed }`). El módulo no contiene textos, códigos ni estados. Listas por dominio: login (`LOGIN_ERROR_RULES`: bloqueo 423/LOCKED con `lockUntil`, credenciales 401, acceso 403 `DISABLED`/`REVOKED`, 429, sin conexión, ≥500), restablecimiento (`RESET_TOKEN_UNAVAILABLE_CODES` + `RESET_ERROR_RULES`), activación (`ACTIVATION_ERROR_RULES`), soporte público (`ACTIVE_TICKET_ERROR_CODES` + `SUPPORT_ERROR_RULES`), alta de Usuarios (`CREATE_USER_ERROR_RULES`, fallback con policy explícita de texto del backend `errorMessage(…, payloadFirst)`). Catálogo: `tools/backend-error-codes.json` (códigos que emite el backend por dominio; `node tools/backend-error-codes.mjs --from ../oniontech`, `--check` para detectar deriva). Las listas sólo nombran códigos del catálogo o fabricados por `core/http.js` (`HTTP_ERROR`, `NETWORK_ERROR`, `REQUEST_TIMEOUT`); el vocabulario que el backend nunca emitió queda fuera (`RESET_TOKEN_EXPIRED`, `RESET_TOKEN_ALREADY_USED`, `TOKEN_INVALID`, `ACTIVE_TICKET_EXISTS`, `PUBLIC_TICKET_OPEN_EXISTS`, `PUBLIC_TICKET_ALREADY_OPEN`, `CREATE_USER_MAIL_FAILED`, sufijos en castellano). Dominios privados (Clientes, Facturas, Incidencias, Usuarios, Home, Cuenta, Servidor, Correo, WhatsApp): policy de texto del backend, explícita en cada llamada `errorMessage(error, fallbackDeDominio, orden)` con su orden nombrado; medido: ninguna vista privada elige texto por código ni por estado (sus `errorStatus`/`errorCode` son control de flujo: 401, cancelaciones, `HOME_CONTEXT_CHANGED`, `MICROSOFT_RECONNECT_REQUIRED`), así que no hay lista de reglas que centralizar; textos curados por código en esos dominios serían un cambio visible y una decisión de producto. Contrato: `tools/error-rules-contract.mjs`; nota `docs/releases/2026-09-16-error-presentation-rules.md`. |
| Superficie exportada (barrido) | `tools/export-surface-contract.mjs` | Dos invariantes. (1) Una exportación existe porque alguien la importa: en los directorios barridos toda exportación tiene consumidor fuera de su módulo (importación estática en `src` o referencia por nombre en `src`, `tools`, `.github`, `docs`, HTML, `vite.config.js` y `package.json`, lo que cubre el acceso por cadena, los fixtures y los módulos que los contratos importan dinámicamente en una página real). (2) El número de exportaciones de cada directorio barrido es igual a su `BASELINE` autorizado: la superficie pública no crece por accidente. Una funcionalidad que necesite una exportación nueva sube el baseline en el mismo commit, a conciencia; un barrido que retira exportaciones lo baja en el mismo commit. El número registra una decisión y se espera que se mueva; el dogma es «no aumentar superficie accidentalmente». Baseline actual: avatar-system 49, chrome 4, Agenda 1, sidebar 7, toast 18, topbar 10, Clientes 102, Correo 25, Cuenta 53, Empleados 2, Facturas 153, Home 75, Incidencias 171, Servidor 127, Usuarios 160, WhatsApp 9. Método por unidad: clasificar cada nombre exportado como A (se usa sólo dentro: se retira el `export`), B (nadie lo usa: se borra), C (API pública deliberada: se documenta) o D (acceso dinámico o incierto: se conserva), verificando estático, dinámico, cadenas, registros, HTML, `main.js`, entradas de Vite, workers, eventos, tests, fixtures, tooling, `.github`, contratos de dist y documentación ejecutable; sólo se actúa sobre A y B de confianza alta, midiendo `dist` y cierres antes y después. Unidades: Servidor, Cuenta, Correo + WhatsApp + sidebar + topbar, Home, Incidencias, Facturas, Clientes, Usuarios, avatar-system, chrome + toast, Agenda + Empleados (`docs/releases/2026-09-16-sweep-*.md`). Un nombre puede ser público por su declaración o por una lista `export { … };` local: en ese segundo caso la exportación es la entrada de la lista, y es la entrada lo que se retira. En un dominio con fachada (`incidencias.api.js` reexporta `export * from` la implementación y difunde su objeto por defecto), retirar un `export` de la implementación estrecha lo que la fachada reexporta pero deja intacto el objeto público: la propiedad sigue existiendo y el consumidor que la usa no se entera. |
| Navegación, guards y commit de vista | `src/router/index.js` | Se conserva un Router y un host de vista comprometida. |
| Cancelación y respuestas vigentes | `src/core/async-scope.js` | Router, EntityOverlay, Correo y consentimiento comparten lifecycle; cada canal identifica su operación más reciente. |
| Carga visual | `src/css/components/skeleton.css` | Una capa global aporta pintura y animaciones; las vistas conservan la geometría necesaria. |
| Tokens y CSS de ruta | `src/css/app.css` y `src/router/styles.js` | No se añade otro cargador global ni otra paleta por pantalla. |
| Marca y páginas públicas | `src/core/public-site.js` | Catálogo consumido por generación estática y navegación SPA. |
| Escritura de metadatos DOM | `src/router/page-metadata.js` | Actualiza título, canonical, robots, Open Graph, Twitter y JSON-LD al navegar. |

Los hosts de avatar proyectan los campos usados por `resolveAvatarPresentation` en `data-avatar-name`, `data-avatar-email`, `data-avatar-user-id` y `data-avatar-username`. El fingerprint y el tono no sustituyen esos aliases: el runtime necesita la identidad original para reconciliar cambios sin perder el email situado en otra celda ni inferir datos de una entidad contigua. Una proyección explícita delimita la identidad completa, incluidos aliases vacíos; sólo los hosts sin metadatos conservan el descubrimiento legacy. Las listas de gestión y los selectores/detalle de Facturas usan esta proyección. Home conserva también userId/username en sus relaciones cuando faltan emails; los demás detalles mantienen sus aliases explícitos. Los IDs de factura y cliente no se utilizan como IDs de usuario. La versión `avatar-identity.v5-user-id-first` prioriza userId, después email y username. Con el mismo userId, cambiar o borrar email/username no altera fingerprint ni tono. La transición cambia una vez el tono de algunos usuarios y conserva la paleta de 20 colores y el hash existentes.

### Sólo habla quien puede responder

`errorMessage` **extrae** el texto que un error lleva dentro; decidir si se le enseña a alguien es de la vista. Un error del backend llega siempre con estado HTTP o con código —los pone `core/http.js`—; una falta de programación no trae ninguno de los dos. Usuarios declara esa política en `humanErrorText`: con estado o código, el error habla con su propio texto; sin ninguno de los dos, manda el texto por defecto que declara cada llamada. Se decide por los hechos que la autoridad ya calcula, **no por el nombre de la clase del error**, para que `CuentaApiError`, `WhatsAppApiError` o `AuthLogoutError` no se queden mudos por parecerse a una falta del motor.

La regla no vive en el núcleo a propósito: `core/errors.js` está en el cierre de arranque de la Home pública, cuyo presupuesto medido deja **33 bytes** (218567 sobre un techo de 218600), y la misma regla escrita allí costaba 91. Una política de presentación de vistas privadas no la paga cada visitante de la web pública. Contrato: `tools/error-extraction-contract.mjs`.

El caso que lo motivó: `usuarios.cursor.js` llamaba a `directoryKey` sin definirlo —el barrido de claves por semántica retiró la copia local, que era `slugKey` letra por letra, clasificándola como muerta cuando tenía dos llamadas vivas— y la vista Usuarios presentaba «directoryKey is not defined» al usuario mientras se quedaba sin datos.

## Contrato de identidad visual

La [simplificación incremental](releases/2026-09-12-private-reduction.md) elimina el postprocesador HTML del alta de Incidencias: su renderer emite directamente todos los aliases, incluidos los vacíos, y delega la normalización en la autoridad existente. No hay otro registro de identidad.

La [consolidación privada del 2026-09-12](releases/2026-09-12-private-centralization.md) incorpora Cuenta al contrato de avatar explícito. Las respuestas de perfil se aplican sólo a su sesión e ID de origen; un GET anterior a una escritura confirmada no puede restaurar datos antiguos. La autoridad privada conserva revisión y operación pendiente, nunca una segunda copia del perfil. Las vistas y cachés de Clientes, Usuarios, Incidencias y Facturas reaccionan a la confirmación mediante sus propietarios existentes.

- `identity.js` resuelve la presentación; `AvatarSystem` reconcilia el DOM. Los IDs de usuario explícitos distintos nunca se unen por email o nombre. Un snapshot sin vínculo de usuario demostrado mantiene el fallback por email/username/nombre; no se inventan IDs ni se crea un registro de correspondencias.
- El solicitante y el técnico del detalle de Incidencias comparten los selectores de identidad de `incidencias-comment-identity` con sus comentarios; el listado también reutiliza la identidad del técnico. Los comentarios se asocian por su ID persistido de evento cuando existe; un autor tercero conserva su userId aunque no haya perfil o foto disponibles. Los constructores iniciales y dinámicos reservan `data-comment-id` para IDs persistidos, sin confundirlos con claves de presentación generadas. Un comentario histórico por email/nombre no adopta el userId de otro perfil por coincidencia de aliases.
- La estabilidad del fingerprint no congela contenido: las firmas de soporte público incluyen los aliases visibles, y comentarios/seguimiento actualizan también aliases vacíos. Las respuestas incompletas del técnico conservan su identidad conocida; un campo explícitamente vacío elimina el dato anterior y una reasignación a otro ID no hereda el perfil anterior.
- El perfil técnico selecciona su foto desde las proyecciones de ticket/Usuarios y delega su estado a AvatarSystem. Se retira `incidencias-technician-avatar-bridge`: copiar una foto de otro host del DOM podía recuperar una imagen obsoleta después de borrarla. Home y Facturas respetan `hasAvatar=false` antes de buscar fuentes antiguas en raw/snapshots.
- La normalización visual del ID no sustituye al identificador de transporte. El perfil técnico conserva el ID original de la entidad de usuario para las peticiones HTTP, incluidas sus mayúsculas; el alias visual normalizado no se usa para inventar un identificador de lectura.
- El backend debe proyectar el userId real cuando ya ha resuelto un vínculo cliente→usuario. El frontend no trata `clienteId`, `facturaId`, `ticketId` o metadata sin procedencia probada como identidad de usuario. Los históricos sin vínculo recuperable quedan como fallback explícito y no se equiparan a perfiles identificados.

### La fotografía vigente no es un dato del documento

Una factura guarda una instantánea fiscal: identidad legal, nombre fiscal, dirección, conceptos, fechas, base, impuestos y total. La fotografía de la persona no pertenece a esa instantánea; es identidad visual vigente. Actualizar la segunda no puede reescribir la primera, y presentar la primera no puede congelar la segunda.

| Responsabilidad | Autoridad | Nota |
| --- | --- | --- |
| Resolver la identidad (nombre, email, userId, username, semilla, tono, iniciales, huella) | `src/features/avatar-system/identity.js` · `resolveAvatarPresentation` | Jerárquica: `user:<id>` > `email:<e>` > `username:<u>` > `name:<n>`. |
| Registrar la fotografía vigente de una identidad | `src/features/avatar-system/index.js` · `applyConfirmedAvatar(identity, photo)` | Única operación añadida. No lee documentos, no escribe dominio, no crea caché por modal. |
| Encontrar a quién repintar | El índice `data-avatar-identity` que el propio sistema ya escribía en cada host gestionado | No hay bus nuevo ni recorrido global de vistas: se consulta la huella y se sincronizan sólo esos hosts. |
| Repintar un host | `synchronizeAvatarHost` | El mismo camino de siempre; la fotografía confirmada entra como una fuente más, por delante de la URL incrustada en el documento. |

- **Consumidores.** Cualquier host gestionado cuya huella coincida: listas y detalle de Incidencias, Facturas, Clientes, Usuarios, Home, barra lateral y superior. No hay lista de superficies que mantener: la pertenencia la decide la identidad, no la pantalla.
- **Quién invalida.** Sólo el flujo real de cambio de foto: `/cuenta` confirma contra `POST`/`DELETE /api/users/avatar` y `src/features/user-profile/index.js` llama a `applyConfirmedAvatar` con lo confirmado. No hay sondeo, ni temporizador, ni `Date.now()` por render, ni reescritura de URLs firmadas.
- **Ciclo de vida.** No se abre ninguna suscripción nueva, así que no hay ninguna que fugarse al cerrar un modal. El registro vive en el módulo y `destroyAvatarSystem` lo vacía.
- **Anti-intuición.** Una fotografía se registra únicamente bajo alias de identificador (`user:`, `email:`, `username:`); nunca bajo nombre. Un host identificado sólo por nombre tiene otra huella y es inalcanzable por esta vía: una factura sin vínculo inequívoco con un perfil conserva su reserva. No se enlazan personas por parecido de nombre.
- **Lo que no toca.** No se relee el documento, no se recalculan importes, no cambian destinatario, nombre fiscal, dirección ni PDF. El recorrido integrado lo comprueba sobre el build real: tras confirmar la foto, la factura muestra la identidad vigente y conserva razón social e importe, con cero escrituras de dominio.

## Autoridad de etiquetas de Incidencias

Un identificador del backend no es una etiqueta en castellano, y saber poner mayúsculas no es traducir. La vista separa cuatro cosas que antes se confundían:

| Concepto | Autoridad | Regla |
| --- | --- | --- |
| Valor técnico | El dato tal cual llega o se envía | Nunca se sustituye por su presentación. |
| Normalización | `normalizeIncidenciaStatus` / `Priority` / `Category` (`incidencias.options.js`, sólo alias) | Declarar una etiqueta no puede mover un valor: las etiquetas no participan en la normalización. |
| Etiqueta de presentación | `labelWith(field, options, aliases, declaredLabels, value, fallback)` | Código conocido → etiqueta canónica. Código desconocido → fallback nombrado del campo («Estado no reconocido», «Prioridad no reconocida», «Tipo no reconocido»), conservando el valor técnico para diagnóstico. Texto libre del usuario → su propio texto, escapado. |
| Agrupación y filtrado | `STATUS_MAP`, `PRIORITY_MAP`, `OPEN_STATUS_KEYS`, `CLOSED_STATUS_KEYS` (`incidencias.template.js`) | Se conservan intactos. La lista agrupa; el dominio nombra. Un estado «En proceso» sigue perteneciendo al grupo abierto sin presentarse como «Abierta». |

Durante la edición no se guarda un fallback visual como nuevo valor del dato: los fallbacks no son escribibles y el contrato lo comprueba. Home es multidominio y conserva su propia tabla: sólo distingue código conocido, código desconocido (no se pinta) y texto libre (se respeta). Contrato: `tools/incidencia-labels-contract.mjs`.

## Contrato de detalle transversal

`EntityOverlay` prepara los módulos y estilos, valida el origen comprometido y posee la cancelación de una sesión. Las factories `createFacturaDetailController`, `createIncidenciaDetailController`, `createClienteDetailController` y `createUsuarioDetailController` conservan los modales reales del dominio en modo `detailOnly`, sin montar listados. La ruta de origen permanece visible y sólo el Router escribe navegación.

Cada apertura tiene una sola invocación inicial a la API de detalle. Incidencias y Facturas conservan el panel conectado en carga, error, reintento y refresco. Estado, live sync y avatares de Incidencias reciben la proyección del controlador; las señales solicitan el refresco a ese mismo propietario. Las listas posponen su reconciliación mientras su detalle está abierto y aplican los cambios al cerrar.

Las entradas, callbacks, contratos y ubicaciones para intervenir están definidos en [UI_MODAL_SYSTEM.md](UI_MODAL_SYSTEM.md). Se retiraron los puentes de apertura de Home/Facturas/Incidencias y los adaptadores de detalle de lectura de Clientes/Usuarios. Los adaptadores contextuales de identidad visual tienen otra responsabilidad y se conservan.

## Contrato de estadísticas

Cabecera y filtros de Facturas reciben la misma proyección en el render inicial y las actualizaciones. Incidencias genera el alcance y el copy desde su modelo tanto en carga como en datos. Se retiran los observers locales y la lectura de cifras desde HTML; el DOM representa el estado, no calcula estadísticas.

El backend determina los totales autorizados. `statistics.js` interpreta valores declarados sin sustituir ausencias por cero ni ignorar mínimos explícitos. `facturas.stats.js` proyecta esos campos para Home y Facturas; las filas paginadas no completan cantidades globales ausentes. Los indicadores de filas cargadas conservan su alcance visible.

La caché estándar de Home pertenece a la sesión y al dashboard sin filtros especiales. Las consultas particulares no la sobrescriben. Las peticiones concurrentes de estadísticas de Facturas se separan por sesión, consulta y revisión de escrituras; una respuesta invalidada no se convierte en la nueva cifra visible. Las facetas de Incidencias invalidan también la petición anterior cuando resuelven desde caché o desde una primera página completa.

## Contrato de errores de autenticación

`src/core/http.js` reintenta una petición privada tras refrescar el token cuando recibe un `401`, salvo que el código pertenezca a `NON_REFRESHABLE_AUTH_CODES`. Esa lista es exactamente el catálogo que el backend emite y que un refresh no puede resolver: `INVALID_CREDENTIALS` (login), sesión inválida, revocada, expirada, inexistente o con usuario/id/token que no coinciden (`SESSION_*`) y cuenta no operativa (`USER_INVALID`, `USER_INACTIVE`, `USER_DISABLED`, `USER_NOT_FOUND`, `USER_EMAIL_UNVERIFIED`). La autoridad es el backend (`config/jwt.js`, `router/auth/refresh.js`, `router/auth/login.impl.js`); ambos repositorios pinan la misma lista con un contrato (`tools/auth-error-code-alignment-contract.mjs` aquí, `tools/auth-error-code-catalog-contracts.mjs` en oniontech). Los códigos que un token nuevo sí resuelve (`TOKEN_EXPIRED`, `MISSING_TOKEN`, `SESSION_REQUIRED`, `TOKEN_VERSION_*`) nunca entran en la lista. `PASSWORD_CHANGE_REQUIRED` llega como `403` y no cierra sesión desde `http.js`.

El login trata el bloqueo temporal por intentos fallidos (`423 ACCOUNT_TEMPORARILY_LOCKED`, con `lockUntil`) con un mensaje propio que indica los minutos de espera, en lugar del mensaje genérico de credenciales.

## Superficie de mutación de facturas

La API de facturas del frontend (`facturas.api.base.js` → `boundary` → `alias-core` → `canonical` → `facturas.api.js`) solo expone las mutaciones que el backend implementa como comandos `POST`: creación (`createFactura`), envío (`sendFactura`) y registro de pago (`markFacturaPaid`). `PUT`, `PATCH` y `DELETE` sobre `/api/facturas/:id` no existen en el backend (404) y se retiraron del frontend el 2026-09-15 junto con el transporte que degradaba `PUT→PATCH→POST` cuando el método no estaba disponible. `tools/facturas-mutation-surface-contract.mjs` impide que reaparezcan sin una ruta backend real y comprueba que ningún módulo de `src/` las consume.

## Errores corregidos

- En Correo, cambiar de carpeta invalida el lector anterior. Una respuesta tardía de mensajes, estado o buzones no puede escribir sobre otra operación ni después de desmontar la vista, incluso cuando el transporte ignora la cancelación.
- Los modales anidados no liberan el scroll ni devuelven el foco mientras otro modal sigue siendo propietario. Escape se procesa una vez y respeta los componentes que ya lo han consumido.
- El sistema de avatares detecta cambios de identidad y sustitución de imagen. Un fallo de carga no impide recuperar la foto al recibir una URL nueva.
- Los indicadores comparten sus animaciones y respetan reducción de movimiento. Las reglas de carga fría, refresco y carga incremental siguen separadas para conservar datos visibles.
- El consentimiento espera su hoja de estilos antes de mostrarse, evitando el salto desde su posición temporal en el documento. Si la hoja falla, los controles siguen disponibles sin estilo. Abrir preferencias durante la carga usa el scope asíncrono compartido y se cancela al cerrar o abandonar la ruta pública.

## Marca e indexación

La portada se titula exactamente **Onion Support**. Su descripción presenta soporte para particulares, autónomos y empresas en España, con asistencia remota y diagnóstico claro. La atención presencial se acuerda según servicio y ubicación; la dirección empresarial real no cambia.

Las cinco páginas de servicios mantienen URLs y canonical propios bajo `https://onionsupport.com`, enlaces HTML de navegación y jerarquía de datos estructurados. `/login` permanece accesible y rastreable, pero declara `noindex, follow` en HTML y cabecera HTTP y no aparece en el sitemap.

Para actualizar el catálogo y sus documentos derivados:

```bash
node tools/sync-public-site.mjs
node tools/sync-public-site.mjs --check
```

No editar los metadatos de cada HTML por separado. Los textos específicos de servicios viven en `tools/public-service-content.mjs`; la generación comparte plantilla, navegación, pie y metadatos.

Google decide los sitelinks y puede reescribir títulos o descripciones. La estructura técnica favorece una marca coherente y servicios subordinados, pero no garantiza una presentación idéntica a Apple ni la retirada inmediata de resultados ya indexados. Después del despliegue corresponde solicitar rastreo de la portada y del sitemap en Search Console.

Referencias oficiales: [sitelinks](https://developers.google.com/search/docs/appearance/sitelinks), [noindex rastreable](https://developers.google.com/search/docs/crawling-indexing/block-indexing), [títulos](https://developers.google.com/search/docs/appearance/title-link).

## Verificación y límites

`npm run validate:ci` ejecuta contratos de fuente, pruebas de carreras asíncronas, compilación reproducible, inventario del artefacto y pruebas de navegador. `npm run test:browser:ui` cubre avatares, modales, carga visual, consentimiento con CSS lento o fallido y transiciones reales de metadatos. Los contratos de avatares comparan la identidad antes y después de sincronizar templates reales de Home, Incidencias, Facturas, Clientes y Usuarios, incluidos solicitante/técnico y los detalles. También cubren cambios y borrado de email con ID fijo, homónimos con IDs diferentes, autores terceros, contenido público actualizado y transporte del ID original durante la hidratación del técnico. Se requiere Chrome/Chromium; puede indicarse su ejecutable con `CHROME_BIN`.

El alcance no es reescribir cada API de dominio ni fusionar todas las máquinas de estado. Las nuevas vistas deben consumir estas autoridades; los controladores históricos conservan reglas de dominio y adaptadores que no son intercambiables. La autenticación, la autorización efectiva y los datos de negocio siguen siendo responsabilidad del backend.
