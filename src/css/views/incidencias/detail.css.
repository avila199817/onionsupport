/* =========================================================
   ONION SUPPORT — INCIDENCIAS DETAIL MODAL
   Archivo: /src/css/views/incidencias/detail.css

   DETAIL MODAL CSS · FINAL 10/10 · FLOW SCROLL MODE

   Responsabilidad:
   - CSS externo del modal de detalle de incidencias.
   - Sin CSS inline.
   - Sin :root local.
   - Sin redefinir tokens globales.
   - Sin header/footer sticky o fixed.
   - Header y footer acompañan el scroll del panel.
   - Optimizado para tokens existentes de variables.css.
   - Sin JS, sin bridges, sin capas paralelas.
========================================================= */


/* =========================================================
   ROOT / RESET LOCAL
========================================================= */

#incidencias-detail-modal-root,
#incidencias-detail-modal-root *,
#incidencias-detail-modal-root *::before,
#incidencias-detail-modal-root *::after {
  box-sizing: border-box;
}

#incidencias-detail-modal-root:empty {
  display: none;
}

#incidencias-detail-modal-root {
  position: relative;
  z-index: var(--z-modal, 80);
}

body.incidencias-modal-open,
body.modal-open {
  overflow: hidden;
  overscroll-behavior: none;
}

.incidencias-modal-hidden-download-link {
  display: none !important;
}

.incidencias-modal-panel [hidden] {
  display: none !important;
}

.incidencias-modal-panel button,
.incidencias-modal-panel input,
.incidencias-modal-panel textarea {
  font: inherit;
}

.incidencias-modal-panel svg {
  flex: 0 0 auto;
}

.incidencias-modal-svg-icon {
  display: block;
  inline-size: 15px;
  block-size: 15px;
}


/* =========================================================
   OVERLAY / PANEL
========================================================= */

.incidencias-modal-overlay {
  position: fixed;
  inset: 0;
  z-index: var(--z-modal, 80);

  display: grid;
  place-items: center;

  padding:
    max(var(--space-md, 16px), var(--app-safe-top, 0px))
    max(var(--space-md, 16px), var(--app-safe-right, 0px))
    max(var(--space-md, 16px), var(--app-safe-bottom, 0px))
    max(var(--space-md, 16px), var(--app-safe-left, 0px));

  background: var(--incidencias-modal-overlay-bg, var(--overlay-bg, rgba(0,0,0,.64)));
  color: var(--text, rgba(255,255,255,.92));

  backdrop-filter: var(--incidencias-modal-overlay-blur, var(--overlay-blur, blur(12px) saturate(1.02)));
  -webkit-backdrop-filter: var(--incidencias-modal-overlay-blur, var(--overlay-blur, blur(12px) saturate(1.02)));

  isolation: isolate;
}

.incidencias-modal-panel {
  position: relative;
  z-index: 1;

  inline-size: var(--incidencias-modal-width, min(1160px, calc(100vw - 40px)));
  max-block-size: min(var(--incidencias-modal-max-height, 92vh), calc(100dvh - 32px));

  overflow: auto;
  overscroll-behavior: contain;
  scrollbar-gutter: stable;

  outline: none;

  border: 1px solid var(--incidencias-modal-border, var(--modal-border, var(--panel-border, var(--border-default, rgba(255,255,255,.12)))));
  border-radius: var(--incidencias-modal-radius, var(--modal-radius, var(--radius-2xl, 24px)));

  background:
    var(--glass-shine, linear-gradient(180deg, rgba(255,255,255,.040), rgba(255,255,255,0) 36%)),
    var(--incidencias-modal-bg, var(--modal-bg, var(--surface-elevated-strong, #3B3B3B)));

  box-shadow: var(--incidencias-modal-shadow, var(--shadow-modal, var(--shadow-xl, 0 42px 104px rgba(0,0,0,.56))));

  scrollbar-width: thin;
  scrollbar-color: var(--scrollbar-thumb, rgba(255,255,255,.20)) transparent;

  contain: layout paint style;

  animation:
    incidenciasModalPanelIn
    var(--duration-normal, .20s)
    var(--ease-out, cubic-bezier(.22,1,.36,1));
}

.incidencias-modal-panel::-webkit-scrollbar {
  inline-size: var(--scrollbar-size, 10px);
}

.incidencias-modal-panel::-webkit-scrollbar-track {
  background: transparent;
}

.incidencias-modal-panel::-webkit-scrollbar-thumb {
  border: 2px solid transparent;
  border-radius: var(--radius-pill, 999px);
  background: var(--scrollbar-thumb, rgba(255,255,255,.20));
  background-clip: padding-box;
}

.incidencias-modal-panel::-webkit-scrollbar-thumb:hover {
  background: var(--scrollbar-thumb-hover, rgba(255,255,255,.34));
  background-clip: padding-box;
}


/* =========================================================
   HEADER / HERO
   IMPORTANTE:
   - No sticky.
   - No fixed.
   - Acompaña el scroll natural del panel.
========================================================= */

.incidencias-modal-header {
  position: relative;
  z-index: 1;

  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: var(--incidencias-modal-header-gap, var(--space-sm, 12px));

  padding: var(--incidencias-modal-header-padding, 18px 18px 14px);

  border-block-end: 1px solid var(--incidencias-modal-header-border, var(--border-soft, rgba(255,255,255,.08)));

  background:
    linear-gradient(180deg, rgba(255,255,255,.035), rgba(255,255,255,0)),
    color-mix(in srgb, var(--incidencias-modal-bg, var(--modal-bg, #3B3B3B)) 96%, transparent);
}

.incidencias-modal-hero {
  display: flex;
  align-items: center;
  gap: var(--space-sm, 12px);

  flex: 1 1 auto;
  min-inline-size: 0;
  max-inline-size: calc(100% - 54px);
  min-block-size: var(--incidencias-modal-avatar-size, 66px);
}

.incidencias-modal-hero-content {
  display: grid;
  align-content: center;
  gap: var(--space-2xs, 4px);

  flex: 1 1 auto;
  min-inline-size: 0;
}

.incidencias-modal-hero-chips {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: var(--space-2xs, 4px);

  min-inline-size: 0;
}

.incidencias-modal-title {
  margin: 0;
  min-inline-size: 0;
  max-inline-size: 100%;

  color: var(--text-strong, #ffffff);
  font-size: var(--incidencias-modal-title-size, clamp(19px, 1.9vw, 27px));
  line-height: var(--incidencias-modal-title-line, 1.14);
  letter-spacing: var(--incidencias-modal-title-letter, -.045em);
  font-weight: var(--weight-black, 800);

  overflow: hidden;
  display: -webkit-box;
  -webkit-line-clamp: var(--incidencias-modal-title-clamp, 2);
  -webkit-box-orient: vertical;

  padding-block-end: var(--incidencias-modal-title-bottom-fix, 3px);
}

.incidencias-modal-updated {
  display: block;
  min-inline-size: 0;

  color: var(--text-dim, rgba(255,255,255,.48));
  font-size: var(--font-sm, 12px);
  line-height: 1.25;
  font-weight: var(--weight-semibold, 600);

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.incidencias-modal-close-btn {
  appearance: none;

  inline-size: var(--btn-height, 42px);
  block-size: var(--btn-height, 42px);
  flex: 0 0 auto;

  display: grid;
  place-items: center;

  border: 1px solid var(--btn-ghost-border, rgba(255,255,255,.10));
  border-radius: var(--radius-md, 14px);

  background: var(--btn-ghost-bg, transparent);
  color: var(--text-strong, #ffffff);

  font-size: 18px;
  line-height: 1;

  cursor: pointer;

  transition:
    background var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    border-color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    opacity var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    transform var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1));
}

.incidencias-modal-close-btn:hover {
  transform: translateY(-1px);
  background: var(--btn-ghost-bg-hover, var(--surface-hover, #3B3B3B));
  border-color: var(--border-strong, rgba(255,255,255,.18));
}

.incidencias-modal-close-btn:active {
  transform: translateY(0) scale(.985);
}

.incidencias-modal-close-btn:disabled {
  cursor: not-allowed;
  opacity: .58;
  transform: none;
}


/* =========================================================
   AVATAR
========================================================= */

.incidencias-modal-avatar {
  position: relative;

  inline-size: var(--incidencias-modal-avatar-size, 66px);
  block-size: var(--incidencias-modal-avatar-size, 66px);
  flex: 0 0 var(--incidencias-modal-avatar-size, 66px);

  overflow: visible;
  cursor: default;
}

.incidencias-modal-avatar-frame {
  position: relative;

  inline-size: 100%;
  block-size: 100%;

  overflow: hidden;
  isolation: isolate;

  border: 1px solid var(--avatar-border, var(--border-soft, rgba(255,255,255,.08)));
  border-radius: var(--incidencias-modal-avatar-radius, 21px);

  background:
    linear-gradient(135deg, var(--avatar-bg, var(--graphite-300, #3B3B3B)), var(--avatar-bg-2, var(--graphite-200, #525252)));

  box-shadow:
    inset 0 1px 0 rgba(255,255,255,.10),
    0 10px 22px rgba(0,0,0,.22);
}

.incidencias-modal-avatar-frame::after {
  content: "";
  position: absolute;
  inset: 0;
  z-index: 2;

  pointer-events: none;
  border-radius: inherit;

  background:
    linear-gradient(180deg, rgba(255,255,255,.14), rgba(0,0,0,.08));
}

.incidencias-modal-avatar-frame img {
  position: absolute;
  inset: 0;
  z-index: 1;

  display: block;
  inline-size: 100%;
  block-size: 100%;

  object-fit: cover;
}

.incidencias-modal-avatar-fallback {
  position: absolute;
  inset: 0;
  z-index: 3;

  display: none;
  place-items: center;

  color: var(--avatar-text, var(--text-on-accent, #ffffff));
  font-size: 18px;
  line-height: 1;
  font-weight: var(--weight-black, 800);
  letter-spacing: .03em;
}

.incidencias-modal-avatar-frame[data-fallback="true"] img,
.incidencias-modal-avatar-frame--fallback img {
  display: none !important;
}

.incidencias-modal-avatar-frame[data-fallback="true"] .incidencias-modal-avatar-fallback,
.incidencias-modal-avatar-frame--fallback .incidencias-modal-avatar-fallback {
  display: grid;
}

.incidencias-modal-avatar--emerald .incidencias-modal-avatar-frame {
  background: linear-gradient(135deg, #16a34a, #14b8a6);
}

.incidencias-modal-avatar--blue .incidencias-modal-avatar-frame {
  background: linear-gradient(135deg, #2563eb, #06b6d4);
}

.incidencias-modal-avatar--amber .incidencias-modal-avatar-frame {
  background: linear-gradient(135deg, #ca8a04, #ea580c);
}

.incidencias-modal-avatar--rose .incidencias-modal-avatar-frame {
  background: linear-gradient(135deg, #e11d48, #ec4899);
}

.incidencias-modal-avatar--purple .incidencias-modal-avatar-frame,
.incidencias-modal-avatar--violet .incidencias-modal-avatar-frame {
  background: var(--gradient-accent, linear-gradient(135deg, #696969, #525252, #3B3B3B));
}

.incidencias-modal-avatar--cyan .incidencias-modal-avatar-frame {
  background: linear-gradient(135deg, #0891b2, #4f46e5);
}

.incidencias-modal-avatar--orange .incidencias-modal-avatar-frame {
  background: linear-gradient(135deg, #f97316, #ef4444);
}


/* =========================================================
   CHIPS
========================================================= */

.incidencias-modal-id-chip,
.incidencias-modal-chip {
  min-block-size: var(--incidencias-modal-chip-height, 24px);
  block-size: var(--incidencias-modal-chip-height, 24px);

  display: inline-flex;
  align-items: center;
  justify-content: center;

  max-inline-size: 220px;
  padding-inline: var(--incidencias-modal-chip-padding-x, 9px);

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;

  border-radius: var(--radius-pill, 999px);

  font-size: var(--incidencias-modal-chip-font-size, 10px);
  line-height: 1;
  font-weight: var(--weight-bold, 700);
  letter-spacing: .055em;
  text-transform: uppercase;
}

.incidencias-modal-id-chip {
  appearance: none;

  border: 1px solid var(--badge-border, rgba(255,255,255,.14));
  background: var(--badge-bg, rgba(255,255,255,.09));
  color: var(--text-muted, rgba(255,255,255,.66));

  cursor: pointer;

  transition:
    background var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    border-color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    transform var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1));
}

.incidencias-modal-id-chip:hover {
  transform: translateY(-1px);
  border-color: var(--border-strong, rgba(255,255,255,.18));
  background: var(--btn-secondary-bg-hover, var(--surface-hover, #3B3B3B));
  color: var(--text-strong, #ffffff);
}

.incidencias-modal-id-chip:active {
  transform: translateY(0) scale(.985);
}

.incidencias-modal-chip {
  border: 1px solid var(--chip-border, rgba(255,255,255,.14));
  background: var(--chip-bg, rgba(255,255,255,.085));
  color: var(--chip-text, var(--text-soft, rgba(255,255,255,.84)));
}

.incidencias-modal-chip--status-open {
  color: var(--accent-contrast, #ffffff);
  background: var(--accent-soft, rgba(255,255,255,.14));
  border-color: var(--accent-border-strong, rgba(255,255,255,.22));
}

.incidencias-modal-chip--status-pending,
.incidencias-modal-chip--priority-medium {
  color: var(--warning, #eab308);
  background: var(--warning-bg, rgba(234,179,8,.085));
  border-color: var(--border-warning, rgba(234,179,8,.28));
}

.incidencias-modal-chip--status-progress,
.incidencias-modal-chip--priority-low {
  color: var(--info-strong, #b8b8b8);
  background: var(--info-bg, rgba(255,255,255,.085));
  border-color: var(--border-info, rgba(255,255,255,.18));
}

.incidencias-modal-chip--status-resolved,
.incidencias-modal-chip--status-closed {
  color: var(--success, #22c55e);
  background: var(--success-bg, rgba(34,197,94,.085));
  border-color: var(--border-success, rgba(34,197,94,.30));
}

.incidencias-modal-chip--priority-high,
.incidencias-modal-chip--priority-critical {
  color: var(--error, #ef4444);
  background: var(--error-bg, rgba(239,68,68,.085));
  border-color: var(--border-error, rgba(239,68,68,.32));
}

.incidencias-modal-chip--status-neutral,
.incidencias-modal-chip--priority-neutral {
  color: var(--text-dim, rgba(255,255,255,.48));
  background: var(--neutral-bg, rgba(255,255,255,.075));
  border-color: var(--neutral-border, rgba(255,255,255,.14));
}


/* =========================================================
   BODY / SECTIONS
========================================================= */

.incidencias-modal-body {
  display: grid;
  gap: var(--incidencias-modal-body-gap, var(--space-lg, 20px));

  min-inline-size: 0;

  padding: var(--incidencias-modal-padding, var(--space-lg, 20px));
}

.incidencias-modal-section-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-sm, 12px);
  flex-wrap: wrap;

  min-inline-size: 0;
}

.incidencias-modal-section-head h3 {
  margin: 0;

  color: var(--text-strong, #ffffff);
  font-size: var(--font-xl, 17px);
  line-height: 1.25;
  letter-spacing: var(--letter-tight, -.032em);
  font-weight: var(--weight-bold, 700);
}

.incidencias-modal-section-head span {
  color: var(--text-dim, rgba(255,255,255,.48));
  font-size: var(--font-xs, 11px);
  line-height: 1.2;
  font-weight: var(--weight-bold, 700);
  letter-spacing: .065em;
  text-transform: uppercase;
}

.incidencias-modal-description-section,
.incidencias-modal-history-section {
  display: grid;
  gap: var(--space-xs, 8px);
  min-inline-size: 0;
}

.incidencias-modal-description-box {
  min-block-size: var(--incidencias-modal-description-min-height, 86px);

  padding: var(--incidencias-modal-description-padding, 16px 18px);

  border: 1px solid var(--incidencias-modal-card-border, var(--border-soft, rgba(255,255,255,.08)));
  border-radius: var(--incidencias-modal-description-radius, 16px);

  background:
    var(--glass-shine, linear-gradient(180deg, rgba(255,255,255,.040), rgba(255,255,255,0) 36%)),
    var(--incidencias-modal-card-bg, var(--block-bg, rgba(255,255,255,.075)));

  color: var(--text-strong, #ffffff);
  font-size: var(--font-base, 14px);
  line-height: var(--incidencias-modal-description-line, 1.55);
  font-weight: var(--weight-semibold, 600);

  white-space: pre-wrap;
  overflow-wrap: anywhere;
}


/* =========================================================
   META GRID
========================================================= */

.incidencias-modal-meta-grid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: var(--incidencias-modal-meta-grid-gap, 10px);

  min-inline-size: 0;
}

.incidencias-modal-meta-card {
  position: relative;

  display: grid;
  align-content: start;
  gap: var(--space-2xs, 4px);

  min-inline-size: 0;
  min-block-size: 78px;

  padding: var(--incidencias-modal-meta-card-padding, 12px);

  overflow: hidden;

  border: 1px solid var(--incidencias-modal-meta-card-border, var(--border-soft, rgba(255,255,255,.08)));
  border-radius: var(--incidencias-modal-meta-card-radius, 16px);

  background:
    var(--glass-shine, linear-gradient(180deg, rgba(255,255,255,.040), rgba(255,255,255,0) 36%)),
    var(--incidencias-modal-meta-card-bg, var(--block-bg, rgba(255,255,255,.075)));

  transition:
    background var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    border-color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    transform var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1));
}

.incidencias-modal-meta-card:hover {
  transform: var(--incidencias-modal-lift-transform, translateY(-2px));
  border-color: var(--incidencias-modal-card-hover-border, rgba(255,255,255,.18));
  background:
    var(--glass-shine, linear-gradient(180deg, rgba(255,255,255,.040), rgba(255,255,255,0) 36%)),
    var(--incidencias-modal-card-hover-bg, rgba(255,255,255,.085));
}

.incidencias-modal-meta-card span {
  min-inline-size: 0;

  color: var(--text-faint, rgba(255,255,255,.30));
  font-size: var(--font-2xs, 10px);
  line-height: 1.2;
  font-weight: var(--weight-bold, 700);
  letter-spacing: .08em;
  text-transform: uppercase;
}

.incidencias-modal-meta-card strong {
  min-inline-size: 0;

  color: var(--text-strong, #ffffff);
  font-size: var(--font-md, 13px);
  line-height: 1.38;
  font-weight: var(--weight-bold, 700);

  overflow-wrap: anywhere;
}


/* =========================================================
   TECHNICIAN VALUE
========================================================= */

.incidencias-modal-technician-inline {
  display: inline-flex;
  align-items: center;
  gap: 8px;

  min-inline-size: 0;
  max-inline-size: 100%;
}

.incidencias-modal-technician-inline strong {
  min-inline-size: 0;

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;

  color: var(--text-strong, #ffffff);
}

.incidencias-modal-technician-avatar {
  position: relative;

  inline-size: 28px;
  block-size: 28px;
  flex: 0 0 28px;

  display: inline-grid;
  place-items: center;

  overflow: hidden;

  border: 1px solid var(--border-soft, rgba(255,255,255,.08));
  border-radius: var(--radius-pill, 999px);

  background: linear-gradient(135deg, var(--avatar-bg, #3B3B3B), var(--avatar-bg-2, #525252));
  color: var(--avatar-text, #ffffff);

  font-size: 10px;
  line-height: 1;
  font-weight: var(--weight-black, 800);
  letter-spacing: -.02em;
}

.incidencias-modal-technician-avatar img {
  position: absolute;
  inset: 0;
  z-index: 1;

  display: block;

  inline-size: 100%;
  block-size: 100%;

  object-fit: cover;
}

.incidencias-modal-technician-avatar > span {
  position: relative;
  z-index: 2;

  display: grid;
  place-items: center;
}

.incidencias-modal-technician-avatar[data-fallback="false"] > span {
  display: none;
}

.incidencias-modal-technician-avatar[data-fallback="true"] img,
.incidencias-modal-technician-avatar--fallback img {
  display: none !important;
}

.incidencias-modal-technician-avatar[data-fallback="true"] > span,
.incidencias-modal-technician-avatar--fallback > span {
  display: grid;
}


/* =========================================================
   FEEDBACK
========================================================= */

.incidencias-modal-feedback {
  display: grid;
  gap: var(--space-2xs, 4px);

  padding: var(--space-sm, 12px) var(--space-md, 16px);

  border: 1px solid var(--panel-border, var(--border-soft, rgba(255,255,255,.08)));
  border-radius: var(--radius-md, 14px);

  background:
    var(--glass-shine, linear-gradient(180deg, rgba(255,255,255,.040), rgba(255,255,255,0) 36%)),
    var(--surface-glass, rgba(255,255,255,.075));
}

.incidencias-modal-feedback strong {
  color: var(--text-strong, #ffffff);
  font-size: var(--font-md, 13px);
  line-height: 1.3;
  font-weight: var(--weight-bold, 700);
}

.incidencias-modal-feedback span {
  color: var(--text-muted, rgba(255,255,255,.66));
  font-size: var(--font-sm, 12px);
  line-height: 1.5;
}

.incidencias-modal-feedback--success {
  border-color: var(--border-success, rgba(34,197,94,.30));
  background: var(--success-bg, rgba(34,197,94,.085));
}

.incidencias-modal-feedback--error {
  border-color: var(--border-error, rgba(239,68,68,.32));
  background: var(--error-bg, rgba(239,68,68,.085));
}

.incidencias-modal-feedback--warning {
  border-color: var(--border-warning, rgba(234,179,8,.28));
  background: var(--warning-bg, rgba(234,179,8,.085));
}

.incidencias-modal-feedback--info {
  border-color: var(--border-accent, rgba(255,255,255,.18));
  background: var(--accent-soft, rgba(255,255,255,.14));
}


/* =========================================================
   PREVIEW
========================================================= */

.incidencias-modal-preview {
  display: grid;
  gap: var(--space-sm, 12px);

  min-inline-size: 0;

  padding: var(--space-md, 16px);

  border: 1px solid var(--incidencias-modal-preview-border, var(--border-default, rgba(255,255,255,.12)));
  border-radius: var(--incidencias-modal-preview-radius, 18px);

  background:
    var(--glass-shine, linear-gradient(180deg, rgba(255,255,255,.040), rgba(255,255,255,0) 36%)),
    var(--incidencias-modal-preview-bg, rgba(255,255,255,.060));
}

.incidencias-modal-preview-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-sm, 12px);
  flex-wrap: wrap;

  min-inline-size: 0;
}

.incidencias-modal-preview-copy {
  display: grid;
  gap: var(--space-3xs, 2px);

  min-inline-size: 0;
}

.incidencias-modal-preview-copy strong {
  color: var(--text-strong, #ffffff);
  font-size: var(--font-md, 13px);
  line-height: 1.35;
  font-weight: var(--weight-bold, 700);

  overflow-wrap: anywhere;
}

.incidencias-modal-preview-copy span {
  color: var(--text-dim, rgba(255,255,255,.48));
  font-size: var(--font-xs, 11px);
  line-height: 1.35;
}

.incidencias-modal-preview-actions {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: var(--space-xs, 8px);
  flex-wrap: wrap;

  min-inline-size: 0;
}

.incidencias-modal-preview-btn {
  appearance: none;

  min-block-size: var(--btn-height-sm, 34px);
  padding-inline: var(--space-sm, 12px);

  border: 1px solid var(--btn-secondary-border, rgba(255,255,255,.14));
  border-radius: var(--radius-sm, 11px);

  background: var(--btn-secondary-bg, #3B3B3B);
  color: var(--btn-secondary-text, var(--text, rgba(255,255,255,.92)));

  font-size: var(--font-sm, 12px);
  line-height: 1;
  font-weight: var(--weight-bold, 700);
  white-space: nowrap;

  cursor: pointer;

  transition:
    background var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    border-color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    transform var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1));
}

.incidencias-modal-preview-btn:hover {
  transform: translateY(-1px);
  background: var(--btn-secondary-bg-hover, #525252);
  border-color: var(--border-strong, rgba(255,255,255,.18));
  color: var(--text-strong, #ffffff);
}

.incidencias-modal-preview-btn:active {
  transform: translateY(0) scale(.985);
}

.incidencias-modal-preview-frame {
  inline-size: 100%;
  block-size: var(--incidencias-modal-preview-height, min(64vh, 720px));
  min-block-size: var(--incidencias-modal-preview-min-height, 430px);

  overflow: hidden;
  isolation: isolate;

  border: 1px solid var(--border-soft, rgba(255,255,255,.08));
  border-radius: var(--incidencias-modal-preview-frame-radius, 15px);

  background: rgba(0,0,0,.14);
}

.incidencias-modal-preview-frame.is-image {
  display: grid;
  place-items: center;
}

.incidencias-modal-preview-iframe {
  display: block;

  inline-size: 100%;
  block-size: 100%;

  border: 0;
  background: #ffffff;
  color-scheme: light;
}

.incidencias-modal-preview-image {
  display: block;

  inline-size: 100%;
  block-size: 100%;

  object-fit: contain;
  background: rgba(0,0,0,.10);
}

.incidencias-modal-preview-note {
  margin: 0;

  color: var(--text-dim, rgba(255,255,255,.48));
  font-size: var(--font-xs, 11px);
  line-height: 1.45;
}


/* =========================================================
   COMPOSER
========================================================= */

.incidencias-modal-composer {
  position: relative;

  display: grid;
  gap: var(--incidencias-modal-composer-gap, 12px);

  min-inline-size: 0;

  padding: var(--incidencias-modal-composer-padding, 14px);

  overflow: hidden;

  border: 1px solid var(--incidencias-modal-composer-border, var(--border-default, rgba(255,255,255,.12)));
  border-radius: var(--incidencias-modal-composer-radius, 18px);

  background:
    var(--glass-shine, linear-gradient(180deg, rgba(255,255,255,.040), rgba(255,255,255,0) 36%)),
    var(--incidencias-modal-composer-bg, rgba(255,255,255,.060));

  transition:
    border-color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    background var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1));
}

.incidencias-modal-composer:focus-within {
  border-color: var(--input-border-focus, rgba(255,255,255,.36));
}

.incidencias-modal-composer-head {
  display: flex;
  align-items: flex-start;
  gap: var(--space-sm, 12px);

  min-inline-size: 0;
}

.incidencias-modal-composer-icon {
  inline-size: 38px;
  block-size: 38px;
  flex: 0 0 38px;

  display: grid;
  place-items: center;

  border: 1px solid var(--border-accent, rgba(255,255,255,.18));
  border-radius: var(--radius-md, 14px);

  background: var(--accent-soft, rgba(255,255,255,.14));
  color: var(--text-strong, #ffffff);

  font-size: 24px;
  line-height: 1;
  font-weight: var(--weight-black, 800);
}

.incidencias-modal-composer-copy {
  display: grid;
  gap: var(--space-2xs, 4px);

  min-inline-size: 0;
}

.incidencias-modal-composer-copy h3 {
  margin: 0;

  color: var(--text-strong, #ffffff);
  font-size: var(--font-xl, 17px);
  line-height: 1.2;
  letter-spacing: var(--letter-tight, -.032em);
  font-weight: var(--weight-bold, 700);
}

.incidencias-modal-composer-copy span,
.incidencias-modal-composer-foot span {
  color: var(--text-muted, rgba(255,255,255,.66));
  font-size: var(--font-sm, 12px);
  line-height: 1.5;
}

.incidencias-modal-comment-textarea {
  appearance: none;

  inline-size: 100%;
  min-block-size: var(--incidencias-modal-textarea-min-height, 138px);

  padding: var(--incidencias-modal-textarea-padding, 15px 16px);

  resize: vertical;

  border: 1px solid var(--input-border, rgba(255,255,255,.14));
  border-radius: var(--incidencias-modal-textarea-radius, var(--input-radius, 15px));

  background: var(--input-bg, #242424);
  color: var(--input-text, var(--text, rgba(255,255,255,.92)));
  caret-color: var(--accent, #696969);

  outline: none;

  font-size: var(--font-base, 14px);
  line-height: var(--incidencias-modal-textarea-line, 1.58);
  font-weight: var(--weight-semibold, 600);

  transition:
    background var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    border-color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    opacity var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1));
}

.incidencias-modal-comment-textarea::placeholder {
  color: var(--input-placeholder, rgba(255,255,255,.42));
  font-weight: var(--weight-semibold, 600);
}

.incidencias-modal-comment-textarea:hover {
  background: var(--input-bg-hover, #3B3B3B);
  border-color: var(--input-border-hover, rgba(255,255,255,.22));
}

.incidencias-modal-comment-textarea:focus {
  background: var(--input-bg-focus, #3B3B3B);
  border-color: var(--input-border-focus, rgba(255,255,255,.36));
  box-shadow: var(--input-shadow-focus, 0 0 0 3px rgba(255,255,255,.12));
}

.incidencias-modal-comment-textarea:disabled {
  cursor: not-allowed;
  opacity: .58;
}

.incidencias-modal-dropzone {
  position: relative;

  min-block-size: 78px;

  display: grid;
  place-items: center;
  gap: var(--space-2xs, 4px);

  padding: var(--incidencias-modal-upload-padding, 14px);

  border: 1px dashed var(--incidencias-modal-upload-border, var(--border-default, rgba(255,255,255,.12)));
  border-radius: var(--incidencias-modal-upload-radius, 16px);

  background: var(--incidencias-modal-upload-bg, rgba(255,255,255,.060));
  color: var(--text-soft, rgba(255,255,255,.84));

  text-align: center;
  cursor: pointer;

  transition:
    background var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    border-color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    transform var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1));
}

.incidencias-modal-dropzone:hover {
  transform: translateY(-1px);
  background: var(--block-bg-hover, rgba(255,255,255,.105));
  border-color: var(--border-strong, rgba(255,255,255,.18));
}

.incidencias-modal-dropzone:focus-within {
  border-color: var(--input-border-focus, rgba(255,255,255,.36));
  box-shadow: var(--focus-ring, 0 0 0 3px rgba(255,255,255,.18));
}

.incidencias-modal-dropzone input {
  position: absolute;

  inline-size: 1px;
  block-size: 1px;

  opacity: 0;
  pointer-events: none;
}

.incidencias-modal-dropzone span {
  color: var(--text-strong, #ffffff);
  font-size: var(--font-md, 13px);
  line-height: 1.3;
  font-weight: var(--weight-bold, 700);
}

.incidencias-modal-dropzone small {
  color: var(--text-dim, rgba(255,255,255,.48));
  font-size: var(--font-xs, 11px);
  line-height: 1.35;
}


/* =========================================================
   UPLOAD / PENDING FILES
========================================================= */

.incidencias-modal-upload-progress {
  display: grid;
  gap: var(--space-xs, 8px);

  padding: var(--space-sm, 12px);

  border: 1px solid var(--border-accent, rgba(255,255,255,.18));
  border-radius: var(--radius-md, 14px);

  background: var(--accent-soft, rgba(255,255,255,.14));
}

.incidencias-modal-upload-progress-head {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-sm, 12px);
}

.incidencias-modal-upload-progress-head strong,
.incidencias-modal-upload-progress-head span {
  color: var(--text-strong, #ffffff);
  font-size: var(--font-sm, 12px);
  line-height: 1.2;
  font-weight: var(--weight-bold, 700);
}

.incidencias-modal-upload-progress-native {
  appearance: none;

  inline-size: 100%;
  block-size: 8px;

  overflow: hidden;

  border: 0;
  border-radius: var(--radius-pill, 999px);

  background: rgba(255,255,255,.10);
}

.incidencias-modal-upload-progress-native::-webkit-progress-bar {
  border-radius: var(--radius-pill, 999px);
  background: rgba(255,255,255,.10);
}

.incidencias-modal-upload-progress-native::-webkit-progress-value {
  border-radius: var(--radius-pill, 999px);
  background: var(--btn-primary-bg, #525252);
}

.incidencias-modal-upload-progress-native::-moz-progress-bar {
  border-radius: var(--radius-pill, 999px);
  background: var(--btn-primary-bg, #525252);
}

.incidencias-modal-upload-progress-meta {
  color: var(--text-dim, rgba(255,255,255,.48));
  font-size: var(--font-xs, 11px);
  line-height: 1.3;
}

.incidencias-modal-pending-empty {
  color: var(--text-dim, rgba(255,255,255,.48));
  font-size: var(--font-sm, 12px);
  line-height: 1.45;
}

.incidencias-modal-pending-list {
  display: grid;
  gap: var(--space-xs, 8px);

  max-block-size: 260px;
  overflow: auto;

  padding-inline-end: 2px;

  scrollbar-width: thin;
  scrollbar-color: var(--scrollbar-thumb, rgba(255,255,255,.20)) transparent;
}

.incidencias-modal-pending-list::-webkit-scrollbar {
  inline-size: var(--scrollbar-size-sm, 6px);
}

.incidencias-modal-pending-list::-webkit-scrollbar-thumb {
  border: 2px solid transparent;
  border-radius: var(--radius-pill, 999px);
  background: var(--scrollbar-thumb, rgba(255,255,255,.20));
  background-clip: padding-box;
}

.incidencias-modal-pending-file {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: var(--space-sm, 12px);

  min-inline-size: 0;

  padding: var(--space-xs, 8px) var(--space-sm, 12px);

  border: 1px solid var(--border-soft, rgba(255,255,255,.08));
  border-radius: var(--radius-md, 14px);

  background: var(--surface-glass, rgba(255,255,255,.075));
}

.incidencias-modal-pending-file > div {
  display: grid;
  gap: var(--space-2xs, 4px);

  min-inline-size: 0;
}

.incidencias-modal-pending-file strong {
  color: var(--text-strong, #ffffff);
  font-size: var(--font-sm, 12px);
  line-height: 1.35;
  font-weight: var(--weight-bold, 700);

  overflow-wrap: anywhere;
}

.incidencias-modal-pending-file span {
  color: var(--text-dim, rgba(255,255,255,.48));
  font-size: var(--font-xs, 11px);
  line-height: 1.35;
}

.incidencias-modal-pending-file button {
  appearance: none;

  min-block-size: var(--btn-height-sm, 34px);
  padding-inline: var(--space-sm, 12px);

  border: 1px solid var(--btn-ghost-border, rgba(255,255,255,.10));
  border-radius: var(--radius-sm, 11px);

  background: var(--btn-ghost-bg, transparent);
  color: var(--btn-ghost-text, var(--text, rgba(255,255,255,.92)));

  font-size: var(--font-sm, 12px);
  line-height: 1;
  font-weight: var(--weight-bold, 700);

  cursor: pointer;

  transition:
    background var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    border-color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    transform var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1));
}

.incidencias-modal-pending-file button:hover {
  transform: translateY(-1px);
  background: var(--btn-ghost-bg-hover, var(--surface-hover, #3B3B3B));
  border-color: var(--border-strong, rgba(255,255,255,.18));
  color: var(--text-strong, #ffffff);
}

.incidencias-modal-pending-file button:active {
  transform: translateY(0) scale(.985);
}


/* =========================================================
   ATTACHMENTS
========================================================= */

.incidencias-modal-files-block,
.incidencias-modal-current-files {
  display: grid;
  gap: var(--space-xs, 8px);

  min-inline-size: 0;
}

.incidencias-modal-files-block--compact {
  margin-block-start: -2px;
}

.incidencias-modal-empty-box {
  padding: var(--space-sm, 12px) var(--space-md, 16px);

  border: 1px solid var(--border-soft, rgba(255,255,255,.08));
  border-radius: var(--radius-md, 14px);

  background: var(--surface-glass, rgba(255,255,255,.075));
  color: var(--text-dim, rgba(255,255,255,.48));

  font-size: var(--font-sm, 12px);
  line-height: 1.45;
}

.incidencias-modal-attachments-grid {
  display: grid;
  gap: var(--space-xs, 8px);

  min-inline-size: 0;

  container-type: inline-size;
}

.incidencias-modal-attachment-card {
  display: block;

  min-inline-size: 0;
  padding: var(--space-xs, 8px);

  border: 1px solid var(--incidencias-modal-card-border, var(--border-soft, rgba(255,255,255,.08)));
  border-radius: var(--incidencias-modal-card-radius, var(--radius-xl, 20px));

  background:
    var(--glass-shine, linear-gradient(180deg, rgba(255,255,255,.040), rgba(255,255,255,0) 36%)),
    var(--incidencias-modal-card-bg, rgba(255,255,255,.060));

  transition:
    background var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    border-color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    transform var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1));
}

.incidencias-modal-attachment-card:hover {
  transform: var(--incidencias-modal-lift-transform, translateY(-2px));
  border-color: var(--incidencias-modal-card-hover-border, rgba(255,255,255,.18));
  background:
    var(--glass-shine, linear-gradient(180deg, rgba(255,255,255,.040), rgba(255,255,255,0) 36%)),
    var(--incidencias-modal-card-hover-bg, rgba(255,255,255,.085));
}

.incidencias-modal-attachment-row {
  display: grid;
  grid-template-columns: var(--incidencias-modal-thumb-width, 104px) minmax(0, 1fr) auto;
  gap: var(--space-sm, 12px);
  align-items: center;

  min-inline-size: 0;
}

.incidencias-modal-attachment-copy {
  display: grid;
  gap: var(--space-2xs, 4px);

  min-inline-size: 0;
  overflow: hidden;
}

.incidencias-modal-attachment-copy strong {
  min-inline-size: 0;

  color: var(--text-strong, #ffffff);
  font-size: var(--font-md, 13px);
  line-height: 1.35;
  font-weight: var(--weight-semibold, 600);

  overflow-wrap: anywhere;
}

.incidencias-modal-attachment-copy span {
  min-inline-size: 0;

  color: var(--text-dim, rgba(255,255,255,.48));
  font-size: var(--font-xs, 11px);
  line-height: 1.4;

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.incidencias-modal-attachment-actions {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: var(--space-xs, 8px);
  flex-wrap: wrap;

  min-inline-size: 0;
}

.incidencias-modal-file-square,
.incidencias-modal-image-thumb-wrap {
  appearance: none;

  position: relative;

  inline-size: var(--incidencias-modal-thumb-width, 104px);
  block-size: var(--incidencias-modal-thumb-height, 78px);

  display: grid;
  place-items: center;

  overflow: hidden;

  padding: 0;

  border: 1px solid var(--incidencias-modal-thumb-border, var(--border-soft, rgba(255,255,255,.08)));
  border-radius: var(--incidencias-modal-thumb-radius, 16px);

  background:
    var(--glass-shine, linear-gradient(180deg, rgba(255,255,255,.040), rgba(255,255,255,0) 36%)),
    var(--incidencias-modal-thumb-bg, rgba(255,255,255,.060));

  color: var(--text-dim, rgba(255,255,255,.48));

  font-size: var(--font-xs, 11px);
  line-height: 1;
  font-weight: var(--weight-black, 800);
  letter-spacing: .08em;

  cursor: pointer;

  transition:
    background var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    border-color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    transform var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1));
}

.incidencias-modal-file-square:hover,
.incidencias-modal-image-thumb-wrap:hover {
  transform: translateY(-2px);
  border-color: var(--incidencias-modal-thumb-hover-border, rgba(255,255,255,.18));
  background: var(--incidencias-modal-card-hover-bg, rgba(255,255,255,.085));
}

.incidencias-modal-file-square:active,
.incidencias-modal-image-thumb-wrap:active {
  transform: translateY(0) scale(.985);
}

.incidencias-modal-file-square--image {
  color: var(--text-strong, #ffffff);
  border-color: var(--border-accent, rgba(255,255,255,.18));
  background: var(--accent-soft, rgba(255,255,255,.14));
}

.incidencias-modal-image-thumb {
  display: block;

  inline-size: 100%;
  block-size: 100%;

  object-fit: cover;
}

.incidencias-modal-image-thumb-fallback {
  display: none;

  color: var(--text-dim, rgba(255,255,255,.48));
  font-size: var(--font-xs, 11px);
  line-height: 1;
  font-weight: var(--weight-black, 800);
  letter-spacing: .08em;
}

.incidencias-modal-image-thumb-wrap[data-thumb-error="true"] .incidencias-modal-image-thumb {
  display: none !important;
}

.incidencias-modal-image-thumb-wrap[data-thumb-error="true"] .incidencias-modal-image-thumb-fallback {
  position: absolute;
  inset: 0;

  display: grid;
  place-items: center;

  background: var(--surface-glass, rgba(255,255,255,.075));
}

.incidencias-modal-image-open-badge {
  position: absolute;
  inset-inline: 6px;
  inset-block-end: 6px;

  min-block-size: 22px;

  display: grid;
  place-items: center;

  border-radius: var(--radius-pill, 999px);

  background: rgba(0,0,0,.58);
  color: #ffffff;

  font-size: 10px;
  line-height: 1;
  font-weight: var(--weight-bold, 700);
  letter-spacing: .04em;
  text-transform: uppercase;

  opacity: 0;
  transform: translateY(4px);

  transition:
    opacity var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    transform var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1));
}

.incidencias-modal-image-thumb-wrap:hover .incidencias-modal-image-open-badge {
  opacity: 1;
  transform: translateY(0);
}

.incidencias-modal-view-btn,
.incidencias-modal-download-btn {
  appearance: none;

  min-block-size: var(--btn-height-sm, 34px);
  padding-inline: var(--space-sm, 12px);

  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-xs, 8px);

  border: 1px solid var(--btn-secondary-border, rgba(255,255,255,.14));
  border-radius: var(--radius-sm, 11px);

  background: var(--btn-secondary-bg, #3B3B3B);
  color: var(--btn-secondary-text, var(--text, rgba(255,255,255,.92)));

  font-size: var(--font-sm, 12px);
  line-height: 1;
  font-weight: var(--weight-bold, 700);

  cursor: pointer;

  transition:
    background var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    border-color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    opacity var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    transform var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1));
}

.incidencias-modal-view-btn:hover,
.incidencias-modal-download-btn:hover {
  transform: translateY(-1px);
  background: var(--btn-secondary-bg-hover, #525252);
  border-color: var(--border-strong, rgba(255,255,255,.18));
  color: var(--text-strong, #ffffff);
}

.incidencias-modal-view-btn:active,
.incidencias-modal-download-btn:active {
  transform: translateY(0) scale(.985);
}

.incidencias-modal-view-btn:disabled,
.incidencias-modal-download-btn:disabled,
.incidencias-modal-view-btn[aria-busy="true"],
.incidencias-modal-download-btn[aria-busy="true"] {
  cursor: wait;
  opacity: .70;
  transform: none;
}

.incidencias-modal-action-icon {
  display: inline-grid;
  place-items: center;

  inline-size: 15px;
  block-size: 15px;
}


/* =========================================================
   TIMELINE
========================================================= */

.incidencias-timeline-empty {
  padding: var(--space-md, 16px);

  border: 1px solid var(--border-soft, rgba(255,255,255,.08));
  border-radius: var(--radius-lg, 17px);

  background: var(--surface-glass, rgba(255,255,255,.075));
  color: var(--text-dim, rgba(255,255,255,.48));

  font-size: var(--font-sm, 12px);
  line-height: 1.45;
}

.incidencias-timeline-list {
  display: grid;
  gap: var(--space-sm, 12px);

  min-inline-size: 0;
}

.incidencias-timeline-card {
  position: relative;

  display: grid;
  grid-template-columns: var(--incidencias-modal-timeline-accent-width, 4px) minmax(0, 1fr) auto;
  gap: var(--incidencias-modal-timeline-gap, 14px);
  align-items: start;

  min-inline-size: 0;

  padding: var(--incidencias-modal-timeline-padding, 14px 16px 14px 0);

  border: 1px solid var(--border-soft, rgba(255,255,255,.08));
  border-radius: var(--incidencias-modal-timeline-radius, 18px);

  background:
    var(--glass-shine, linear-gradient(180deg, rgba(255,255,255,.040), rgba(255,255,255,0) 36%)),
    var(--surface-glass, rgba(255,255,255,.075));

  transition:
    background var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    border-color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    transform var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1));
}

.incidencias-timeline-card:hover {
  transform: translateY(-2px);
  border-color: var(--incidencias-modal-card-hover-border, rgba(255,255,255,.18));
  background: var(--incidencias-modal-card-hover-bg, rgba(255,255,255,.085));
}

.incidencias-timeline-card.is-comment {
  border-color: var(--border-success, rgba(34,197,94,.30));
  background: var(--success-bg, rgba(34,197,94,.085));
}

.incidencias-timeline-card.is-created {
  opacity: .92;
}

.incidencias-timeline-accent {
  inline-size: var(--incidencias-modal-timeline-accent-width, 4px);
  min-block-size: 100%;

  border-radius: var(--radius-pill, 999px);
  background: var(--accent, #696969);
}

.incidencias-timeline-card.is-comment .incidencias-timeline-accent {
  background: var(--success, #22c55e);
}

.incidencias-timeline-card.is-created .incidencias-timeline-accent {
  background: var(--text-faint, rgba(255,255,255,.30));
}

.incidencias-timeline-main {
  display: grid;
  gap: var(--space-xs, 8px);

  min-inline-size: 0;
}

.incidencias-timeline-title-row {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: var(--space-xs, 8px);

  min-inline-size: 0;
}

.incidencias-timeline-title {
  color: var(--text-strong, #ffffff);
  font-size: var(--font-md, 13px);
  line-height: 1.3;
  font-weight: var(--weight-bold, 700);

  overflow-wrap: anywhere;
}

.incidencias-timeline-kind {
  min-block-size: 22px;

  display: inline-flex;
  align-items: center;

  padding-inline: 8px;

  border: 1px solid var(--border-soft, rgba(255,255,255,.08));
  border-radius: var(--radius-pill, 999px);

  background: var(--badge-bg, rgba(255,255,255,.09));
  color: var(--text-dim, rgba(255,255,255,.48));

  font-size: 10px;
  line-height: 1;
  font-weight: var(--weight-bold, 700);
  letter-spacing: .045em;
  text-transform: uppercase;
}

.incidencias-timeline-body {
  margin: 0;

  min-inline-size: 0;

  color: var(--text-soft, rgba(255,255,255,.84));
  font-size: var(--font-md, 13px);
  line-height: 1.58;

  white-space: pre-wrap;
  overflow-wrap: anywhere;
}

.incidencias-timeline-meta {
  display: grid;
  justify-items: end;
  gap: var(--space-2xs, 4px);

  min-inline-size: 150px;
}

.incidencias-timeline-meta strong {
  color: var(--text-soft, rgba(255,255,255,.84));
  font-size: var(--font-xs, 11px);
  line-height: 1.25;
  font-weight: var(--weight-semibold, 600);
  text-align: end;
}

.incidencias-timeline-meta span {
  color: var(--text-dim, rgba(255,255,255,.48));
  font-size: var(--font-xs, 11px);
  line-height: 1.25;
  text-align: end;
  font-variant-numeric: tabular-nums;
}


/* =========================================================
   FOOTER / SUBMIT
   IMPORTANTE:
   - No sticky.
   - No fixed.
   - No negative margins.
   - Acompaña el scroll natural del panel.
========================================================= */

.incidencias-modal-footer {
  position: relative;
  z-index: 1;

  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: var(--modal-footer-gap, var(--space-sm, 12px));

  margin: 0;
  padding-block-start: var(--space-xs, 8px);

  border: 0;
  background: transparent;
}

.incidencias-modal-submit-btn {
  appearance: none;

  min-block-size: var(--incidencias-modal-footer-button-height, var(--btn-height, 42px));
  padding-inline: var(--space-lg, 20px);

  border: 1px solid var(--btn-primary-border, rgba(255,255,255,.14));
  border-radius: var(--incidencias-modal-footer-button-radius, var(--btn-radius, 15px));

  background: var(--btn-primary-bg, #525252);
  color: var(--btn-primary-text, #ffffff);

  font-size: var(--font-md, 13px);
  line-height: 1;
  font-weight: var(--weight-bold, 700);

  cursor: pointer;

  box-shadow: var(--btn-primary-shadow, 0 12px 28px rgba(0,0,0,.24));

  transition:
    background var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    border-color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    color var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    opacity var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1)),
    transform var(--duration-fast, .12s) var(--ease-standard, cubic-bezier(.2,.8,.2,1));
}

.incidencias-modal-submit-btn:hover {
  transform: translateY(-1px);
  background: var(--btn-primary-bg-hover, #696969);
}

.incidencias-modal-submit-btn:active {
  transform: translateY(0) scale(.985);
}

.incidencias-modal-submit-btn:disabled,
.incidencias-modal-submit-btn[aria-busy="true"],
.incidencias-modal-submit-btn.is-loading {
  cursor: wait;
  opacity: .82;
  transform: none;
}


/* =========================================================
   LOADING / SPINNERS
========================================================= */

.incidencias-modal-loading-overlay {
  position: absolute;
  inset: 0;
  z-index: 8;

  display: grid;
  place-items: center;

  padding: var(--space-lg, 20px);

  background: color-mix(in srgb, var(--incidencias-modal-bg, var(--modal-bg, #3B3B3B)) 82%, transparent);
}

.incidencias-modal-loading-box {
  display: grid;
  justify-items: center;
  gap: var(--space-xs, 8px);

  min-inline-size: min(100%, 240px);

  padding: var(--space-lg, 20px) var(--space-xl, 26px);

  border: 1px solid var(--panel-border, rgba(255,255,255,.10));
  border-radius: var(--radius-xl, 20px);

  background: var(--popover-bg, var(--surface-elevated-strong, #3B3B3B));
  box-shadow: var(--shadow-lg, 0 26px 64px rgba(0,0,0,.42));
}

.incidencias-modal-loading-box > span,
.incidencias-modal-inline-spinner > span {
  flex: 0 0 auto;

  border-radius: var(--radius-pill, 999px);
  border-style: solid;
  border-color: color-mix(in srgb, currentColor 22%, transparent);
  border-block-start-color: currentColor;

  animation: incidenciasModalSpin .78s linear infinite;
}

.incidencias-modal-loading-box > span {
  inline-size: 26px;
  block-size: 26px;
  border-width: 3px;

  color: var(--accent, #696969);
}

.incidencias-modal-loading-box strong {
  color: var(--text-strong, #ffffff);
  font-size: var(--font-md, 13px);
  line-height: 1.25;
  font-weight: var(--weight-bold, 700);
}

.incidencias-modal-inline-spinner {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-xs, 8px);

  white-space: nowrap;
}

.incidencias-modal-inline-spinner > span {
  inline-size: 14px;
  block-size: 14px;
  border-width: 2px;
}


/* =========================================================
   GLOBAL STATES / ACCESSIBILITY
========================================================= */

#incidencias-detail-modal-root [aria-busy="true"] {
  cursor: wait;
}

#incidencias-detail-modal-root button[disabled],
#incidencias-detail-modal-root button[aria-disabled="true"],
#incidencias-detail-modal-root textarea[disabled],
#incidencias-detail-modal-root input[disabled] {
  opacity: .62;
}

.incidencias-modal-id-chip:focus-visible,
.incidencias-modal-close-btn:focus-visible,
.incidencias-modal-preview-btn:focus-visible,
.incidencias-modal-dropzone:focus-within,
.incidencias-modal-pending-file button:focus-visible,
.incidencias-modal-file-square:focus-visible,
.incidencias-modal-image-thumb-wrap:focus-visible,
.incidencias-modal-view-btn:focus-visible,
.incidencias-modal-download-btn:focus-visible,
.incidencias-modal-submit-btn:focus-visible,
.incidencias-modal-comment-textarea:focus-visible {
  outline: none;
  box-shadow: var(--focus-ring, 0 0 0 3px rgba(255,255,255,.18));
}

.incidencias-modal-panel ::selection {
  background: var(--selection-bg, rgba(255,255,255,.28));
  color: var(--selection-text, #ffffff);
}


/* =========================================================
   LIGHT THEME
========================================================= */

[data-theme="light"] .incidencias-modal-overlay {
  background: var(--incidencias-modal-overlay-bg, var(--overlay-bg, rgba(15,23,42,.25)));
}

[data-theme="light"] .incidencias-modal-panel {
  background:
    var(--glass-shine, linear-gradient(180deg, rgba(255,255,255,.82), rgba(255,255,255,0) 34%)),
    var(--incidencias-modal-bg, var(--modal-bg, rgba(255,255,255,.965)));

  border-color: var(--panel-border, rgba(23,32,51,.074));
  box-shadow:
    var(--shadow-xl, 0 28px 64px rgba(15,23,42,.12)),
    inset 0 0 0 1px rgba(255,255,255,.62);
}

[data-theme="light"] .incidencias-modal-header {
  background:
    linear-gradient(180deg, rgba(255,255,255,.72), rgba(255,255,255,.42)),
    var(--incidencias-modal-bg, rgba(255,255,255,.965));
  border-color: var(--border-soft, rgba(23,32,51,.074));
}

[data-theme="light"] .incidencias-modal-description-box,
[data-theme="light"] .incidencias-modal-meta-card,
[data-theme="light"] .incidencias-modal-composer,
[data-theme="light"] .incidencias-modal-attachment-card,
[data-theme="light"] .incidencias-timeline-card,
[data-theme="light"] .incidencias-modal-preview,
[data-theme="light"] .incidencias-modal-feedback,
[data-theme="light"] .incidencias-modal-empty-box,
[data-theme="light"] .incidencias-modal-pending-file {
  background:
    linear-gradient(180deg, rgba(255,255,255,.72), rgba(255,255,255,.52)),
    rgba(255,255,255,.76);
}

[data-theme="light"] .incidencias-modal-preview-iframe {
  background: #ffffff;
}

[data-theme="light"] .incidencias-modal-image-open-badge {
  background: rgba(15,23,42,.62);
}

[data-theme="light"] .incidencias-modal-upload-progress-native,
[data-theme="light"] .incidencias-modal-upload-progress-native::-webkit-progress-bar {
  background: rgba(23,32,51,.08);
}

[data-theme="light"] .incidencias-modal-loading-overlay {
  background: color-mix(in srgb, var(--incidencias-modal-bg, rgba(255,255,255,.965)) 82%, transparent);
}

[data-theme="light"] .incidencias-modal-loading-box {
  background: var(--popover-bg, rgba(255,255,255,.975));
  box-shadow:
    var(--shadow-lg, 0 18px 40px rgba(15,23,42,.10)),
    inset 0 0 0 1px rgba(255,255,255,.56);
}


/* =========================================================
   RESPONSIVE
========================================================= */

@media (max-width: 980px) {
  .incidencias-modal-meta-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .incidencias-modal-attachment-row {
    grid-template-columns: var(--incidencias-modal-thumb-width, 82px) minmax(0, 1fr);
  }

  .incidencias-modal-attachment-actions {
    grid-column: 1 / -1;
    justify-content: flex-start;
  }
}

@container (min-width: 820px) {
  .incidencias-modal-attachments-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  .incidencias-modal-attachment-row {
    grid-template-columns: var(--incidencias-modal-thumb-width, 82px) minmax(0, 1fr);
  }

  .incidencias-modal-attachment-actions {
    grid-column: 1 / -1;
    justify-content: flex-start;
  }
}

@media (max-width: 720px) {
  .incidencias-modal-overlay {
    padding:
      max(10px, var(--app-safe-top, 0px))
      max(10px, var(--app-safe-right, 0px))
      max(10px, var(--app-safe-bottom, 0px))
      max(10px, var(--app-safe-left, 0px));
  }

  .incidencias-modal-panel {
    inline-size: 100%;
    max-block-size: calc(100dvh - 20px);
    border-radius: var(--radius-xl, 20px);
  }

  .incidencias-modal-header {
    padding: var(--space-md, 16px);
  }

  .incidencias-modal-body {
    padding: var(--space-md, 16px);
    gap: var(--space-md, 16px);
  }

  .incidencias-modal-hero {
    align-items: center;
    gap: var(--space-sm, 12px);
    max-inline-size: calc(100% - 48px);
  }

  .incidencias-modal-title {
    font-size: clamp(18px, 5.4vw, 23px);
    line-height: 1.18;
  }

  .incidencias-modal-preview-frame {
    block-size: var(--incidencias-modal-preview-height, 54vh);
    min-block-size: var(--incidencias-modal-preview-min-height, 280px);
  }

  .incidencias-timeline-card {
    grid-template-columns: var(--incidencias-modal-timeline-accent-width, 4px) minmax(0, 1fr);
  }

  .incidencias-timeline-meta {
    grid-column: 2;
    justify-items: start;
    min-inline-size: 0;
  }

  .incidencias-timeline-meta strong,
  .incidencias-timeline-meta span {
    text-align: start;
  }

  .incidencias-modal-preview-actions,
  .incidencias-modal-attachment-actions {
    inline-size: 100%;
  }

  .incidencias-modal-preview-btn,
  .incidencias-modal-view-btn,
  .incidencias-modal-download-btn {
    flex: 1 1 auto;
  }
}

@media (max-width: 640px) {
  .incidencias-modal-meta-grid {
    grid-template-columns: 1fr;
  }

  .incidencias-modal-composer-head {
    gap: var(--space-xs, 8px);
  }

  .incidencias-modal-composer-icon {
    inline-size: 32px;
    block-size: 32px;
    flex-basis: 32px;

    border-radius: var(--radius-sm, 11px);

    font-size: 20px;
  }

  .incidencias-modal-comment-textarea {
    min-block-size: var(--incidencias-modal-textarea-min-height, 128px);
    font-size: var(--font-md, 13px);
  }

  .incidencias-modal-pending-file {
    flex-direction: column;
    align-items: flex-start;
  }

  .incidencias-modal-pending-file button {
    inline-size: 100%;
  }

  .incidencias-modal-footer {
    display: grid;
    grid-template-columns: 1fr;
  }

  .incidencias-modal-submit-btn {
    inline-size: 100%;
  }
}

@media (max-width: 480px) {
  .incidencias-modal-header {
    align-items: flex-start;
  }

  .incidencias-modal-avatar {
    inline-size: 52px;
    block-size: 52px;
    flex-basis: 52px;
  }

  .incidencias-modal-hero {
    align-items: flex-start;
    max-inline-size: calc(100% - 46px);
  }

  .incidencias-modal-hero-chips {
    gap: 5px;
  }

  .incidencias-modal-id-chip,
  .incidencias-modal-chip {
    max-inline-size: 100%;
  }

  .incidencias-modal-preview-head {
    align-items: flex-start;
  }

  .incidencias-modal-preview-btn {
    inline-size: 100%;
  }

  .incidencias-modal-attachment-row {
    grid-template-columns: 1fr;
  }

  .incidencias-modal-file-square,
  .incidencias-modal-image-thumb-wrap {
    inline-size: 100%;
    block-size: 156px;
  }

  .incidencias-modal-attachment-actions {
    display: grid;
    grid-template-columns: 1fr;
  }

  .incidencias-modal-view-btn,
  .incidencias-modal-download-btn {
    inline-size: 100%;
  }
}


/* =========================================================
   SUPPORTS / FALLBACKS
========================================================= */

@supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
  .incidencias-modal-overlay {
    backdrop-filter: none;
    -webkit-backdrop-filter: none;
    background: rgba(8,8,10,.82);
  }
}

@supports not (color: color-mix(in srgb, #fff 50%, transparent)) {
  .incidencias-modal-header,
  .incidencias-modal-loading-overlay {
    background: var(--incidencias-modal-bg, var(--modal-bg, #3B3B3B));
  }

  .incidencias-modal-feedback--info,
  .incidencias-modal-file-square--image,
  .incidencias-modal-upload-progress {
    background: var(--accent-soft, rgba(255,255,255,.14));
  }
}

@media (forced-colors: active) {
  .incidencias-modal-panel,
  .incidencias-modal-meta-card,
  .incidencias-modal-description-box,
  .incidencias-modal-composer,
  .incidencias-modal-attachment-card,
  .incidencias-timeline-card,
  .incidencias-modal-preview,
  .incidencias-modal-feedback,
  .incidencias-modal-id-chip,
  .incidencias-modal-chip,
  .incidencias-modal-view-btn,
  .incidencias-modal-download-btn,
  .incidencias-modal-submit-btn,
  .incidencias-modal-preview-btn,
  .incidencias-modal-close-btn {
    forced-color-adjust: auto;
    border-color: CanvasText;
  }

  .incidencias-timeline-accent,
  .incidencias-modal-loading-box > span,
  .incidencias-modal-inline-spinner > span {
    forced-color-adjust: none;
    border-color: Highlight;
  }
}


/* =========================================================
   MOTION
========================================================= */

@keyframes incidenciasModalSpin {
  to {
    transform: rotate(360deg);
  }
}

@keyframes incidenciasModalPanelIn {
  from {
    opacity: 0;
    transform: translateY(10px) scale(.986);
  }

  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

@media (prefers-reduced-motion: reduce) {
  .incidencias-modal-panel,
  .incidencias-modal-close-btn,
  .incidencias-modal-id-chip,
  .incidencias-modal-preview-btn,
  .incidencias-modal-meta-card,
  .incidencias-modal-comment-textarea,
  .incidencias-modal-dropzone,
  .incidencias-modal-pending-file button,
  .incidencias-modal-attachment-card,
  .incidencias-modal-file-square,
  .incidencias-modal-image-thumb-wrap,
  .incidencias-modal-image-open-badge,
  .incidencias-modal-view-btn,
  .incidencias-modal-download-btn,
  .incidencias-timeline-card,
  .incidencias-modal-submit-btn,
  .incidencias-modal-loading-box > span,
  .incidencias-modal-inline-spinner > span {
    animation: none !important;
    transition: none !important;
  }

  .incidencias-modal-close-btn:hover,
  .incidencias-modal-id-chip:hover,
  .incidencias-modal-preview-btn:hover,
  .incidencias-modal-meta-card:hover,
  .incidencias-modal-dropzone:hover,
  .incidencias-modal-pending-file button:hover,
  .incidencias-modal-attachment-card:hover,
  .incidencias-modal-file-square:hover,
  .incidencias-modal-image-thumb-wrap:hover,
  .incidencias-modal-view-btn:hover,
  .incidencias-modal-download-btn:hover,
  .incidencias-timeline-card:hover,
  .incidencias-modal-submit-btn:hover {
    transform: none !important;
  }
}
