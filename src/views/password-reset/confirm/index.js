/* =========================================================
   Onion SPA - Reset Password Confirm Entry
   Archivo: src/views/password-reset/confirm/index.js

   Responsabilidad:
   - Entry point mínimo del confirm reset.
   - Reexportar confirmView.js.
   - Sin lógica.
   - Sin Auth.
   - Sin Router.
   - Sin HTTP.
   - Sin Store.
   - Sin Toast.
   - Sin DOM.
========================================================= */

export {
  default,
  default as ConfirmResetPasswordView,
} from "./confirmView.js";
