# CSS centralization checkpoint — 2026-08-31

This checkpoint records the state of the SPA-wide CSS centralization work after the working session stopped before its intermediate local changes were published to GitHub.

Authoritative follow-up: issue #394, `CSS centralizado: continuar auditoría 1:1 light/dark`.

## Release scope

This checkpoint does not invent or reconstruct unpublished CSS changes. The production release associated with this checkpoint intentionally rebuilds and redeploys the latest revision that is actually stored in GitHub, through the repository's normal validated Azure Static Web Apps release path.

## Remaining work

Continue issue #394 before declaring the CSS centralization effort complete: finish Usuarios and Servidor, close remaining Crear drift, strengthen permanent contracts, perform exhaustive light/dark validation, verify fallback-avatar parity, and remove residual local overrides or duplicated visual authorities.
