/* =========================================================
   ONION SUPPORT — INCIDENCIAS CREATE MODAL
   Archivo: /src/css/views/incidencias/create.css

   CREATE MODAL CSS · EXTREME Z-INDEX · PRODUCTIVE SPA
   FULL APP OVERLAY · VIEW STACKING FIX · SINGLE PANEL SCROLL

   Responsabilidad:
   - CSS externo del modal de creación de incidencias.
   - Modal por encima de view, topbar, sidebar y shells del layout.
   - Corrige stacking context cuando el modal se renderiza dentro de la vista.
   - Sin CSS inline.
   - Sin JS.
   - Sin Store.
   - Sin Router.
   - Sin dependencias externas.
   - Compatible con incidencias.template.create.js.
========================================================= */


/* =========================================================
   STACKING CONTRACT · MODALS OVER EVERYTHING
========================================================= */

:root {
  --inc-create-z-root: 2147483000;
  --inc-create-z-overlay: 2147483001;
  --inc-create-z-panel: 2147483002;
  --inc-create-z-loading: 2147483003;

  --inc-create-topbar-safe: 0px;
  --inc-create-sidebar-safe: 0px;
}

/*
  El modal se renderiza dentro de la view, no en un portal al body.
  Por eso elevamos cualquier ancestro típico que contenga el root.
*/
body:has(#incidencias-create-modal-root) :where(
  .app,
  .app-root,
  .app-shell,
  .app-layout,
  .layout,
  .layout-root,
  .layout-shell,
  .dashboard-layout,
  .main-layout,
  .workspace,
  .workspace-root,
  .workspace-shell,
  .content,
  .content-root,
  .content-shell,
  .main,
  .main-root,
  .main-shell,
  .page,
  .page-root,
  .page-shell,
  .view,
  .view-root,
  .view-shell,
  .panel,
  .panel-root,
  .panel-shell,
  .panel-content,
  .incidencias-view,
  .incidencias-view-root,
  [data-view="incidencias"],
  [data-incidencias-scope]
):has(#incidencias-create-modal-root) {
  position: relative !important;
  z-index: var(--inc-create-z-root) !important;
  overflow: visible !important;
  isolation: auto !important;
  contain: none !important;
  transform-style: flat !important;
}

body:has(#incidencias-detail-modal-root) :where(
  .app,
  .app-root,
  .app-shell,
  .app-layout,
  .layout,
  .layout-root,
  .layout-shell,
  .dashboard-layout,
  .main-layout,
  .workspace,
  .workspace-root,
  .workspace-shell,
  .content,
  .content-root,
  .content-shell,
  .main,
  .main-root,
  .main-shell,
  .page,
  .page-root,
  .page-shell,
  .view,
  .view-root,
  .view-shell,
  .panel,
  .panel-root,
  .panel-shell,
  .panel-content,
  .incidencias-view,
  .incidencias-view-root,
  [data-view="incidencias"],
  [data-incidencias-scope]
):has(#incidencias-detail-modal-root) {
  position: relative !important;
  z-index: var(--inc-create-z-root) !important;
  overflow: visible !important;
  isolation: auto !important;
  contain: none !important;
  transform-style: flat !important;
}

body.incidencias-create-open,
body.incidencias-modal-open,
body.modal-open,
body:has(#incidencias-create-modal-root),
body:has(#incidencias-detail-modal-root) {
  overflow: hidden !important;
  overscroll-behavior: none !important;
  touch-action: none;
}

/* Normalización de ambos roots de modal. */
#incidencias-create-modal-root,
#incidencias-detail-modal-root {
  position: fixed !important;
  inset: 0 !important;

  inline-size: 100vw !important;
  block-size: 100vh !important;
  inline-size: 100dvw !important;
  block-size: 100dvh !important;

  z-index: var(--inc-create-z-root) !important;

  display: block !important;

  margin: 0 !important;
  padding: 0 !important;

  overflow: visible !important;

  pointer-events: auto !important;

  isolation: isolate !important;
  contain: none !important;
  transform: none !important;
  filter: none !important;
  clip-path: none !important;

  font-family: var(--font-family, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif);
}

#incidencias-create-modal-root:empty,
#incidencias-detail-modal-root:empty {
  display: none !important;
  pointer-events: none !important;
}

#incidencias-create-modal-root,
#incidencias-create-modal-root *,
#incidencias-create-modal-root *::before,
#incidencias-create-modal-root *::after {
  box-sizing: border-box;
}

#incidencias-create-modal-root * {
  min-inline-size: 0;
}

#incidencias-create-modal-root [hidden] {
  display: none !important;
}

#incidencias-create-modal-root button,
#incidencias-create-modal-root input,
#incidencias-create-modal-root textarea,
#incidencias-create-modal-root select {
  font: inherit;
}

#incidencias-create-modal-root svg {
  display: block;
  flex: 0 0 auto;
}

#incidencias-create-modal-root button,
#incidencias-create-modal-root input,
#incidencias-create-modal-root textarea,
#incidencias-create-modal-root select,
#incidencias-create-modal-root [role="button"] {
  -webkit-tap-highlight-color: transparent;
}


/* =========================================================
   SCOPED TOKENS
========================================================= */

.inc-create-overlay,
.inc-create-panel {
  --ic-width: min(980px, calc(100vw - 44px));
  --ic-height: min(92dvh, 940px);

  --ic-pad-x: clamp(18px, 2vw, 30px);
  --ic-pad-y: clamp(18px, 2vw, 28px);

  --ic-radius: var(--radius-2xl, 28px);
  --ic-radius-inner: var(--radius-xl, 20px);
  --ic-radius-control: var(--radius-lg, 17px);
  --ic-radius-pill: 999px;

  --ic-text: var(--text, rgba(255, 255, 255, .92));
  --ic-text-strong: var(--text-strong, #ffffff);
  --ic-text-soft: var(--text-soft, rgba(255, 255, 255, .82));
  --ic-text-muted: var(--text-muted, rgba(255, 255, 255, .66));
  --ic-text-dim: var(--text-dim, rgba(255, 255, 255, .46));

  --ic-accent: var(--accent, #8b8b8b);
  --ic-accent-active: var(--accent-active, #ffffff);

  --ic-overlay-bg:
    radial-gradient(circle at 78% 10%, rgba(34, 211, 238, .12), transparent 34%),
    radial-gradient(circle at 16% 82%, rgba(250, 204, 21, .10), transparent 32%),
    linear-gradient(180deg, rgba(0, 0, 0, .66), rgba(0, 0, 0, .82));

  --ic-panel-bg:
    radial-gradient(circle at 16% 0%, rgba(255, 255, 255, .10), transparent 34%),
    linear-gradient(180deg, rgba(255, 255, 255, .090), rgba(255, 255, 255, .030)),
    rgba(25, 25, 25, .94);

  --ic-panel-bg-strong:
    radial-gradient(circle at 18% 0%, rgba(255, 255, 255, .13), transparent 34%),
    linear-gradient(180deg, rgba(255, 255, 255, .12), rgba(255, 255, 255, .042)),
    rgba(34, 34, 34, .96);

  --ic-card-bg:
    radial-gradient(circle at 12% 0%, rgba(255, 255, 255, .10), transparent 34%),
    linear-gradient(180deg, rgba(255, 255, 255, .072), rgba(255, 255, 255, .028)),
    rgba(255, 255, 255, .052);

  --ic-card-bg-hover:
    radial-gradient(circle at 12% 0%, rgba(255, 255, 255, .14), transparent 34%),
    linear-gradient(180deg, rgba(255, 255, 255, .10), rgba(255, 255, 255, .038)),
    rgba(255, 255, 255, .075);

  --ic-input-bg:
    linear-gradient(180deg, rgba(255, 255, 255, .045), rgba(255, 255, 255, .020)),
    rgba(17, 17, 17, .72);

  --ic-input-bg-hover:
    linear-gradient(180deg, rgba(255, 255, 255, .060), rgba(255, 255, 255, .026)),
    rgba(20, 20, 20, .78);

  --ic-input-bg-focus:
    linear-gradient(180deg, rgba(255, 255, 255, .075), rgba(255, 255, 255, .030)),
    rgba(22, 22, 22, .84);

  --ic-border: var(--panel-border, var(--border-default, rgba(255, 255, 255, .14)));
  --ic-border-soft: var(--border-soft, rgba(255, 255, 255, .085));
  --ic-border-strong: var(--border-strong, rgba(255, 255, 255, .22));

  --ic-shadow-panel:
    0 44px 118px rgba(0, 0, 0, .64),
    0 14px 42px rgba(0, 0, 0, .38),
    inset 0 1px 0 rgba(255, 255, 255, .085);

  --ic-shadow-card:
    0 12px 28px rgba(0, 0, 0, .24),
    inset 0 1px 0 rgba(255, 255, 255, .060);

  --ic-shadow-control:
    inset 0 1px 0 rgba(255, 255, 255, .050),
    0 6px 16px rgba(0, 0, 0, .16);

  --ic-focus:
    0 0 0 3px rgba(255, 255, 255, .16),
    0 0 0 7px rgba(96, 165, 250, .13);

  --ic-ease: cubic-bezier(.16, 1, .3, 1);
  --ic-ease-standard: cubic-bezier(.2, .8, .2, 1);
  --ic-fast: .14s;
  --ic-normal: .22s;
}


/* =========================================================
   OVERLAY
========================================================= */

.inc-create-overlay {
  position: fixed !important;
  inset: 0 !important;
  z-index: var(--inc-create-z-overlay) !important;

  display: grid !important;
  place-items: center !important;

  inline-size: 100vw !important;
  block-size: 100vh !important;
  inline-size: 100dvw !important;
  block-size: 100dvh !important;

  padding:
    max(16px, env(safe-area-inset-top))
    max(16px, env(safe-area-inset-right))
    max(16px, env(safe-area-inset-bottom))
    max(16px, env(safe-area-inset-left)) !important;

  overflow: hidden !important;
  overscroll-behavior: none !important;

  background: var(--ic-overlay-bg) !important;
  color: var(--ic-text);

  backdrop-filter: blur(18px) saturate(1.08) !important;
  -webkit-backdrop-filter: blur(18px) saturate(1.08) !important;

  pointer-events: auto !important;
  touch-action: none !important;

  isolation: isolate !important;
  contain: none !important;

  animation: incidenciasCreateOverlayIn .14s var(--ic-ease-standard) both;
}

.inc-create-overlay::before {
  content: "";

  position: absolute;
  inset: 0;
  z-index: 0;

  pointer-events: none;

  background:
    radial-gradient(circle at 50% 0%, rgba(255, 255, 255, .08), transparent 34%),
    linear-gradient(90deg, rgba(0, 0, 0, .22), transparent 18%, transparent 82%, rgba(0, 0, 0, .22));

  opacity: .92;
}

.inc-create-overlay::after {
  content: "";

  position: absolute;
  inset: 0;
  z-index: 0;

  pointer-events: none;

  background:
    linear-gradient(rgba(255, 255, 255, .018) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255, 255, 255, .014) 1px, transparent 1px);

  background-size: 42px 42px;

  opacity: .14;
  mask-image: radial-gradient(circle at 50% 45%, black, transparent 72%);
}


/* =========================================================
   PANEL · SINGLE SCROLL · ABOVE VIEW
========================================================= */

.inc-create-panel {
  position: relative !important;
  z-index: var(--inc-create-z-panel) !important;

  inline-size: var(--ic-width) !important;
  max-inline-size: 100% !important;
  max-block-size: var(--ic-height) !important;

  overflow-x: hidden !important;
  overflow-y: auto !important;
  overscroll-behavior: contain !important;
  scrollbar-gutter: stable;

  outline: none;

  border: 1px solid var(--ic-border) !important;
  border-radius: var(--ic-radius) !important;

  background: var(--ic-panel-bg) !important;
  color: var(--ic-text) !important;

  box-shadow: var(--ic-shadow-panel) !important;

  scrollbar-width: thin;
  scrollbar-color: rgba(255, 255, 255, .24) transparent;

  pointer-events: auto !important;
  isolation: isolate !important;
  background-clip: padding-box;
  contain: layout paint style;

  transform: translateZ(0);

  animation: incidenciasCreatePanelIn .24s var(--ic-ease) both;
}

.inc-create-panel::before {
  content: "";

  position: sticky;
  inset-block-start: 0;
  inset-inline: 0;
  z-index: 30;

  display: block;

  block-size: 1px;

  pointer-events: none;

  background:
    linear-gradient(
      90deg,
      transparent,
      rgba(255, 255, 255, .24),
      rgba(34, 211, 238, .22),
      transparent
    );
}

.inc-create-panel::after {
  content: "";

  position: absolute;
  inset-inline: 18px;
  inset-block-start: 0;
  z-index: 0;

  block-size: 120px;

  pointer-events: none;

  background:
    radial-gradient(circle at 18% 0%, rgba(255, 255, 255, .12), transparent 44%),
    linear-gradient(180deg, rgba(255, 255, 255, .065), transparent);

  opacity: .82;
}

.inc-create-panel.is-submitting {
  overflow: hidden !important;
}

.inc-create-panel.is-submitting .inc-create-form {
  pointer-events: none;
  user-select: none;
}

.inc-create-panel::-webkit-scrollbar {
  inline-size: 10px;
}

.inc-create-panel::-webkit-scrollbar-track {
  background: transparent;
}

.inc-create-panel::-webkit-scrollbar-thumb {
  border: 2px solid transparent;
  border-radius: 999px;
  background: rgba(255, 255, 255, .24);
  background-clip: padding-box;
}

.inc-create-panel::-webkit-scrollbar-thumb:hover {
  background: rgba(255, 255, 255, .38);
  background-clip: padding-box;
}


/* =========================================================
   LOADING OVERLAY
========================================================= */

.inc-create-loading-overlay {
  position: absolute !important;
  inset: 0 !important;
  z-index: var(--inc-create-z-loading) !important;

  display: grid;
  place-items: center;

  padding: 26px;

  background:
    radial-gradient(circle at 50% 30%, rgba(255, 255, 255, .07), transparent 38%),
    rgba(0, 0, 0, .56);

  backdrop-filter: blur(12px) saturate(1.06);
  -webkit-backdrop-filter: blur(12px) saturate(1.06);
}

.inc-create-loading-card {
  position: relative;

  inline-size: min(100%, 310px);
  min-block-size: 168px;

  display: grid;
  align-content: center;
  justify-items: center;
  gap: 16px;

  padding: 26px;

  border: 1px solid rgba(255, 255, 255, .16);
  border-radius: 24px;

  background:
    radial-gradient(circle at 30% 0%, rgba(255, 255, 255, .14), transparent 42%),
    linear-gradient(180deg, rgba(255, 255, 255, .10), rgba(255, 255, 255, .040)),
    rgba(30, 30, 30, .94);

  box-shadow:
    0 26px 72px rgba(0, 0, 0, .54),
    inset 0 1px 0 rgba(255, 255, 255, .08);

  text-align: center;
}

.inc-create-loading-card strong {
  color: var(--ic-text-strong);
  font-size: 14px;
  line-height: 1.44;
  font-weight: 900;
  letter-spacing: -.026em;
}

.inc-create-loading-spinner,
.inc-create-spinner,
.inc-create-mini-spinner {
  flex: 0 0 auto;

  border-radius: 999px;
  border-style: solid;

  animation: incidenciasCreateSpin .78s linear infinite;
}

.inc-create-loading-spinner {
  inline-size: 34px;
  block-size: 34px;

  border-width: 3px;
  border-color: rgba(255, 255, 255, .18);
  border-block-start-color: #ffffff;
}

.inc-create-spinner,
.inc-create-mini-spinner {
  inline-size: 14px;
  block-size: 14px;

  border-width: 2px;
  border-color: color-mix(in srgb, currentColor 24%, transparent);
  border-block-start-color: currentColor;
}


/* =========================================================
   HEADER
========================================================= */

.inc-create-header {
  position: relative;
  z-index: 2;

  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  align-items: start;
  gap: 22px;

  min-inline-size: 0;

  padding:
    clamp(24px, 2.8vw, 36px)
    var(--ic-pad-x)
    clamp(20px, 2vw, 26px);

  border-block-end: 1px solid var(--ic-border-soft);

  background:
    radial-gradient(circle at 0% 0%, rgba(255, 255, 255, .10), transparent 40%),
    linear-gradient(180deg, rgba(255, 255, 255, .080), rgba(255, 255, 255, .022));
}

.inc-create-header-copy {
  display: grid;
  gap: 8px;

  min-inline-size: 0;
}

.inc-create-header-copy h2 {
  margin: 0;

  max-inline-size: 780px;

  color: var(--ic-text-strong);
  font-size: clamp(42px, 4.45vw, 68px);
  line-height: .90;
  letter-spacing: -.070em;
  font-weight: 950;

  text-wrap: balance;

  text-shadow:
    0 1px 0 rgba(255, 255, 255, .08),
    0 20px 44px rgba(0, 0, 0, .40);
}

.inc-create-header-copy p {
  margin: 0;

  max-inline-size: 78ch;

  color: var(--ic-text-muted);
  font-size: 13px;
  line-height: 1.56;
  font-weight: 700;

  text-wrap: pretty;
}

.inc-create-close {
  appearance: none;

  inline-size: 50px;
  block-size: 50px;

  display: grid;
  place-items: center;

  padding: 0;

  border: 1px solid rgba(255, 255, 255, .16);
  border-radius: 20px;

  background:
    radial-gradient(circle at 30% 0%, rgba(255, 255, 255, .14), transparent 42%),
    linear-gradient(180deg, rgba(255, 255, 255, .11), rgba(255, 255, 255, .040)),
    rgba(255, 255, 255, .065);

  color: rgba(255, 255, 255, .86);

  box-shadow:
    var(--ic-shadow-control),
    0 10px 24px rgba(0, 0, 0, .22);

  cursor: pointer;

  transition:
    transform var(--ic-fast) var(--ic-ease),
    border-color var(--ic-fast) var(--ic-ease-standard),
    background var(--ic-fast) var(--ic-ease-standard),
    color var(--ic-fast) var(--ic-ease-standard),
    box-shadow var(--ic-fast) var(--ic-ease-standard),
    opacity var(--ic-fast) var(--ic-ease-standard);
}

.inc-create-close svg {
  inline-size: 19px;
  block-size: 19px;
}

.inc-create-close:hover {
  transform: translateY(-2px);

  border-color: rgba(255, 255, 255, .30);

  background:
    radial-gradient(circle at 30% 0%, rgba(255, 255, 255, .20), transparent 42%),
    linear-gradient(180deg, rgba(255, 255, 255, .15), rgba(255, 255, 255, .052)),
    rgba(255, 255, 255, .090);

  color: #ffffff;

  box-shadow:
    var(--ic-shadow-control),
    0 16px 34px rgba(0, 0, 0, .30);
}

.inc-create-close:active {
  transform: translateY(0) scale(.985);
}

.inc-create-close:disabled {
  pointer-events: none;
  opacity: .52;
}


/* =========================================================
   BODY / FORM
========================================================= */

.inc-create-body {
  position: relative;
  z-index: 2;

  display: grid;
  gap: 16px;

  min-inline-size: 0;

  padding:
    clamp(20px, 2vw, 26px)
    var(--ic-pad-x)
    clamp(26px, 2.7vw, 36px);
}

.inc-create-form {
  display: grid;
  align-items: stretch;
  gap: 16px;

  min-inline-size: 0;
}

.inc-create-grid {
  display: grid;
  gap: 16px;

  min-inline-size: 0;
}

.inc-create-grid--2 {
  grid-template-columns: repeat(2, minmax(0, 1fr));
}

.inc-create-grid--2 > * {
  min-inline-size: 0;
}


/* =========================================================
   ALERTS
========================================================= */

.inc-create-alert {
  position: relative;
  isolation: isolate;

  display: grid;
  grid-template-columns: auto minmax(0, 1fr);
  gap: 12px;
  align-items: center;

  min-inline-size: 0;

  padding: 13px 15px;

  overflow: hidden;

  border: 1px solid rgba(255, 255, 255, .14);
  border-radius: 20px;

  background: var(--ic-card-bg);

  box-shadow: var(--ic-shadow-card);
}

.inc-create-alert::before {
  content: "";

  position: absolute;
  inset-block: 0;
  inset-inline-start: 0;

  inline-size: 3px;

  background: rgba(255, 255, 255, .62);
}

.inc-create-alert-icon {
  inline-size: 34px;
  block-size: 34px;

  display: grid;
  place-items: center;

  border: 1px solid rgba(255, 255, 255, .16);
  border-radius: 999px;

  background: rgba(255, 255, 255, .080);
  color: var(--ic-text-soft);

  box-shadow: inset 0 1px 0 rgba(255, 255, 255, .06);
}

.inc-create-alert-icon svg {
  inline-size: 16px;
  block-size: 16px;
}

.inc-create-alert-copy {
  display: grid;
  gap: 2px;

  min-inline-size: 0;
}

.inc-create-alert-copy strong {
  color: var(--ic-text-strong);
  font-size: 13px;
  line-height: 1.34;
  font-weight: 900;

  overflow-wrap: anywhere;
}

.inc-create-alert-copy span {
  color: var(--ic-text-muted);
  font-size: 12px;
  line-height: 1.45;
  font-weight: 700;

  overflow-wrap: anywhere;
}

.inc-create-alert.is-success {
  border-color: rgba(34, 197, 94, .30);
  background:
    radial-gradient(circle at 0% 0%, rgba(34, 197, 94, .16), transparent 42%),
    rgba(34, 197, 94, .075);
}

.inc-create-alert.is-success::before {
  background: #22c55e;
}

.inc-create-alert.is-success .inc-create-alert-icon {
  border-color: rgba(34, 197, 94, .30);
  background: rgba(34, 197, 94, .10);
  color: #22c55e;
}

.inc-create-alert.is-error {
  border-color: rgba(239, 68, 68, .34);
  background:
    radial-gradient(circle at 0% 0%, rgba(239, 68, 68, .16), transparent 42%),
    rgba(239, 68, 68, .075);
}

.inc-create-alert.is-error::before {
  background: #ef4444;
}

.inc-create-alert.is-error .inc-create-alert-icon {
  border-color: rgba(239, 68, 68, .34);
  background: rgba(239, 68, 68, .10);
  color: #ef4444;
}


/* =========================================================
   BLOCKS / CARDS
========================================================= */

.inc-create-block,
.inc-create-files-card {
  position: relative;
  isolation: isolate;

  display: grid;
  gap: 13px;

  min-inline-size: 0;

  padding: 16px;

  overflow: hidden;

  border: 1px solid rgba(255, 255, 255, .13);
  border-radius: 24px;

  background: var(--ic-card-bg);

  box-shadow: var(--ic-shadow-card);

  contain: layout paint;

  transition:
    background var(--ic-fast) var(--ic-ease-standard),
    border-color var(--ic-fast) var(--ic-ease-standard),
    box-shadow var(--ic-fast) var(--ic-ease-standard),
    transform var(--ic-fast) var(--ic-ease);
}

.inc-create-block::before,
.inc-create-files-card::before {
  content: "";

  position: absolute;
  inset-block: 0;
  inset-inline-start: 0;

  inline-size: 3px;

  background:
    linear-gradient(180deg, #22d3ee, #60a5fa);

  opacity: .72;
}

.inc-create-block:hover,
.inc-create-files-card:hover,
.inc-create-block:focus-within,
.inc-create-files-card:focus-within {
  border-color: rgba(255, 255, 255, .20);
  background: var(--ic-card-bg-hover);

  box-shadow:
    0 16px 34px rgba(0, 0, 0, .28),
    inset 0 1px 0 rgba(255, 255, 255, .070);
}

.inc-create-block--user-search,
.inc-create-block--target {
  border-color: rgba(255, 255, 255, .16);
}

.inc-create-block-head {
  position: relative;
  z-index: 1;

  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 12px;

  min-inline-size: 0;
}

.inc-create-block-head > div {
  display: grid;
  gap: 3px;

  min-inline-size: 0;
}

.inc-create-block-head strong,
.inc-create-files-head strong {
  display: inline-flex;
  align-items: center;
  gap: 8px;

  min-inline-size: 0;

  color: var(--ic-text-strong);
  font-size: 13px;
  line-height: 1.3;
  font-weight: 950;
  letter-spacing: -.020em;
}

.inc-create-block-head strong svg,
.inc-create-files-head strong svg {
  inline-size: 16px;
  block-size: 16px;
}

.inc-create-block-head span {
  color: var(--ic-text-muted);
  font-size: 11px;
  line-height: 1.42;
  font-weight: 700;

  overflow-wrap: anywhere;
}


/* =========================================================
   FIELDS
========================================================= */

.inc-create-field {
  position: relative;
  z-index: 1;

  display: grid;
  gap: 8px;

  min-inline-size: 0;
}

.inc-create-label {
  display: inline-flex;
  align-items: center;
  gap: 6px;

  color: var(--ic-text-soft);
  font-size: 11px;
  line-height: 1;
  font-weight: 950;
  letter-spacing: .080em;
  text-transform: uppercase;
}

.inc-create-label::after {
  content: "";

  inline-size: 4px;
  block-size: 4px;

  border-radius: 999px;
  background: currentColor;
  opacity: .38;
}

.inc-create-input,
.inc-create-textarea,
.inc-create-select {
  appearance: none;

  inline-size: 100%;
  min-inline-size: 0;

  border: 1px solid rgba(255, 255, 255, .14);
  border-radius: var(--ic-radius-control);

  background: var(--ic-input-bg);
  color: var(--ic-text);
  caret-color: #ffffff;

  box-shadow: var(--ic-shadow-control);

  outline: none;

  font-size: 14px;
  line-height: 1.44;
  font-weight: 700;

  transition:
    background var(--ic-fast) var(--ic-ease-standard),
    border-color var(--ic-fast) var(--ic-ease-standard),
    color var(--ic-fast) var(--ic-ease-standard),
    box-shadow var(--ic-fast) var(--ic-ease-standard),
    opacity var(--ic-fast) var(--ic-ease-standard);
}

.inc-create-input,
.inc-create-select {
  min-block-size: 46px;
  padding-inline: 15px;
}

.inc-create-textarea {
  min-block-size: 138px;

  padding: 13px 15px;

  line-height: 1.58;
  resize: vertical;

  scrollbar-width: thin;
  scrollbar-color: rgba(255, 255, 255, .22) transparent;
}

.inc-create-textarea::-webkit-scrollbar {
  inline-size: 10px;
}

.inc-create-textarea::-webkit-scrollbar-thumb {
  border: 2px solid transparent;
  border-radius: 999px;
  background: rgba(255, 255, 255, .22);
  background-clip: padding-box;
}

.inc-create-input::placeholder,
.inc-create-textarea::placeholder {
  color: rgba(255, 255, 255, .42);
}

.inc-create-input:hover,
.inc-create-textarea:hover,
.inc-create-select:hover {
  border-color: rgba(255, 255, 255, .22);
  background: var(--ic-input-bg-hover);
}

.inc-create-input:focus,
.inc-create-textarea:focus,
.inc-create-select:focus {
  border-color: rgba(125, 211, 252, .42);
  background: var(--ic-input-bg-focus);

  box-shadow:
    0 0 0 3px rgba(96, 165, 250, .14),
    var(--ic-shadow-control);
}

.inc-create-input.is-error,
.inc-create-textarea.is-error,
.inc-create-select.is-error {
  border-color: rgba(239, 68, 68, .48);

  box-shadow:
    0 0 0 3px rgba(239, 68, 68, .13),
    var(--ic-shadow-control);
}

.inc-create-input:disabled,
.inc-create-textarea:disabled,
.inc-create-select:disabled {
  cursor: not-allowed;
  opacity: .62;
}

.inc-create-error {
  display: inline-flex;
  align-items: center;
  gap: 6px;

  min-block-size: 18px;

  color: #f87171;
  font-size: 11px;
  line-height: 1.4;
  font-weight: 800;
}

.inc-create-error::before {
  content: "";

  inline-size: 6px;
  block-size: 6px;

  border-radius: 999px;
  background: currentColor;
}


/* =========================================================
   SELECT / SEARCH CONTROL
========================================================= */

.inc-create-select-wrap,
.inc-create-search-control,
.inc-create-search-input-wrap {
  position: relative;

  display: block;

  min-inline-size: 0;
}

.inc-create-input--with-icon,
.inc-create-user-search-input {
  padding-inline-start: 42px;
}

.inc-create-search-icon,
.inc-create-search-input-icon,
.inc-create-select-chevron {
  position: absolute;
  inset-block: 0;
  z-index: 2;

  display: grid;
  place-items: center;

  color: rgba(255, 255, 255, .50);

  pointer-events: none;
}

.inc-create-search-icon,
.inc-create-search-input-icon {
  inset-inline-start: 14px;
}

.inc-create-search-icon svg,
.inc-create-search-input-icon svg {
  inline-size: 15px;
  block-size: 15px;
}

.inc-create-select-chevron {
  inset-inline-end: 14px;

  font-size: 18px;
  line-height: 1;
  font-weight: 950;
}

.inc-create-select {
  padding-inline-end: 42px;
  cursor: pointer;
}

.inc-create-select-wrap:focus-within .inc-create-select-chevron,
.inc-create-search-control:focus-within .inc-create-search-icon {
  color: #7dd3fc;
}


/* =========================================================
   USER SEARCH · TEMPLATE-COMPAT
========================================================= */

.inc-create-target-error-slot:empty,
.inc-create-user-search-slot:empty,
.inc-create-selected-user-slot:empty {
  display: none;
}

.inc-create-user-search-slot,
.inc-create-selected-user-slot {
  position: relative;
  z-index: 1;

  display: grid;
  gap: 8px;

  min-inline-size: 0;
}

.inc-create-user-search-state,
.inc-create-search-state {
  display: flex;
  align-items: center;
  gap: 8px;

  min-inline-size: 0;

  padding: 9px 12px;

  border: 1px solid rgba(255, 255, 255, .14);
  border-radius: 17px;

  background:
    linear-gradient(180deg, rgba(255, 255, 255, .060), rgba(255, 255, 255, .026)),
    rgba(255, 255, 255, .055);

  color: var(--ic-text-muted);

  box-shadow: var(--ic-shadow-control);

  font-size: 12px;
  line-height: 1.44;
  font-weight: 700;
}

.inc-create-user-search-state svg,
.inc-create-search-state svg {
  inline-size: 15px;
  block-size: 15px;
}

.inc-create-user-search-state.is-error,
.inc-create-search-state.is-error {
  border-color: rgba(239, 68, 68, .34);
  background: rgba(239, 68, 68, .075);
  color: #f87171;
}

.inc-create-user-results,
.inc-create-search-results {
  display: grid;
  gap: 8px;

  max-block-size: min(360px, 42dvh);

  overflow: auto;

  padding-inline-end: 2px;

  scrollbar-width: thin;
  scrollbar-color: rgba(255, 255, 255, .22) transparent;
}

.inc-create-user-results::-webkit-scrollbar,
.inc-create-search-results::-webkit-scrollbar {
  inline-size: 10px;
}

.inc-create-user-results::-webkit-scrollbar-thumb,
.inc-create-search-results::-webkit-scrollbar-thumb {
  border: 2px solid transparent;
  border-radius: 999px;
  background: rgba(255, 255, 255, .22);
  background-clip: padding-box;
}

.inc-create-user-result,
.inc-create-search-item {
  appearance: none;

  position: relative;

  display: grid;
  grid-template-columns: auto minmax(0, 1fr);
  align-items: center;
  gap: 12px;

  inline-size: 100%;
  min-inline-size: 0;

  padding: 11px 13px;

  overflow: hidden;

  border: 1px solid rgba(255, 255, 255, .14);
  border-radius: 20px;

  background:
    linear-gradient(180deg, rgba(255, 255, 255, .060), rgba(255, 255, 255, .026)),
    rgba(255, 255, 255, .055);

  color: inherit;

  box-shadow: var(--ic-shadow-control);

  text-align: start;
  cursor: pointer;

  transition:
    transform var(--ic-fast) var(--ic-ease),
    border-color var(--ic-fast) var(--ic-ease-standard),
    background var(--ic-fast) var(--ic-ease-standard),
    opacity var(--ic-fast) var(--ic-ease-standard);
}

.inc-create-user-result:hover,
.inc-create-search-item:hover {
  transform: translateY(-1px);

  border-color: rgba(255, 255, 255, .22);

  background:
    linear-gradient(180deg, rgba(255, 255, 255, .082), rgba(255, 255, 255, .032)),
    rgba(255, 255, 255, .075);
}

.inc-create-user-result:active,
.inc-create-search-item:active {
  transform: translateY(0) scale(.985);
}

.inc-create-user-result:disabled,
.inc-create-search-item:disabled {
  pointer-events: none;
  opacity: .55;
}

.inc-create-user-result[aria-selected="true"],
.inc-create-user-result.is-selected,
.inc-create-search-item[aria-selected="true"],
.inc-create-search-item.is-selected {
  border-color: rgba(125, 211, 252, .34);
  background: rgba(56, 189, 248, .10);
}

.inc-create-user-result-copy,
.inc-create-search-item-copy {
  position: relative;
  z-index: 1;

  display: grid;
  gap: 2px;

  min-inline-size: 0;
}

.inc-create-user-result-copy strong,
.inc-create-search-item-copy strong {
  min-inline-size: 0;

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;

  color: var(--ic-text-strong);
  font-size: 13px;
  line-height: 1.34;
  font-weight: 900;
}

.inc-create-user-result-copy span,
.inc-create-search-item-copy span {
  min-inline-size: 0;

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;

  color: var(--ic-text-muted);
  font-size: 11px;
  line-height: 1.34;
  font-weight: 700;
}


/* =========================================================
   AVATARS · CREATE USER
========================================================= */

.inc-create-user-avatar,
.inc-create-search-avatar {
  position: relative;
  z-index: 1;

  inline-size: 42px;
  block-size: 42px;
  min-inline-size: 42px;
  min-block-size: 42px;

  display: grid;
  place-items: center;

  overflow: hidden;
  isolation: isolate;

  border: 1px solid rgba(255, 255, 255, .10);
  border-radius: 999px;

  background:
    radial-gradient(circle at 30% 16%, rgba(255, 255, 255, .28), transparent 34%),
    linear-gradient(135deg, #64748b, #27272a);

  color: #ffffff;

  font-size: 12px;
  line-height: 1;
  font-weight: 950;
  letter-spacing: -.030em;

  box-shadow:
    0 8px 18px rgba(0, 0, 0, .22),
    inset 0 1px 0 rgba(255, 255, 255, .09);
}

.inc-create-user-avatar::after,
.inc-create-search-avatar::after {
  content: "";

  position: absolute;
  inset: 0;
  z-index: 4;

  border-radius: inherit;

  pointer-events: none;

  background:
    radial-gradient(circle at 30% 22%, rgba(255, 255, 255, .28), transparent 34%),
    linear-gradient(180deg, rgba(255, 255, 255, .10), rgba(0, 0, 0, .10));
}

.inc-create-user-avatar img,
.inc-create-search-avatar img {
  position: absolute;
  inset: 0;
  z-index: 2;

  display: block;

  inline-size: 100%;
  block-size: 100%;

  object-fit: cover;
  object-position: center;
}

.inc-create-user-avatar > span,
.inc-create-search-avatar-fallback,
.inc-create-search-avatar > span {
  position: relative;
  z-index: 3;

  display: grid;
  place-items: center;

  inline-size: 100%;
  block-size: 100%;

  color: #ffffff;
}

.inc-create-user-avatar.has-image > span,
.inc-create-search-avatar.has-image .inc-create-search-avatar-fallback,
.inc-create-search-avatar.has-image > span {
  display: none;
}

.inc-create-user-avatar.is-fallback img,
.inc-create-user-avatar:not(.has-image) img,
.inc-create-user-avatar img[src=""],
.inc-create-user-avatar img:not([src]),
.inc-create-search-avatar[data-fallback="true"] img,
.inc-create-search-avatar:not(.has-image) img,
.inc-create-search-avatar img[src=""],
.inc-create-search-avatar img:not([src]) {
  display: none !important;
}

.inc-create-target-user-avatar {
  inline-size: 44px;
  block-size: 44px;
  min-inline-size: 44px;
  min-block-size: 44px;
}


/* =========================================================
   SELECTED USER CARD · TEMPLATE-COMPAT
========================================================= */

.inc-create-selected-user,
.inc-create-target-user-card {
  position: relative;

  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  align-items: center;
  gap: 12px;

  min-inline-size: 0;

  padding: 11px 13px;

  overflow: hidden;

  border: 1px solid rgba(125, 211, 252, .28);
  border-radius: 20px;

  background:
    radial-gradient(circle at 0% 0%, rgba(56, 189, 248, .14), transparent 42%),
    rgba(56, 189, 248, .075);

  box-shadow: var(--ic-shadow-control);
}

.inc-create-selected-user::before,
.inc-create-target-user-card::before {
  content: "";

  position: absolute;
  inset-block: 0;
  inset-inline-start: 0;

  inline-size: 3px;

  background: #38bdf8;
}

.inc-create-selected-user-main {
  position: relative;
  z-index: 1;

  display: grid;
  grid-template-columns: auto minmax(0, 1fr);
  align-items: center;
  gap: 12px;

  min-inline-size: 0;
}

.inc-create-selected-user-copy,
.inc-create-target-user-copy {
  position: relative;
  z-index: 1;

  display: grid;
  gap: 2px;

  min-inline-size: 0;
}

.inc-create-selected-user-copy strong,
.inc-create-target-user-copy strong {
  min-inline-size: 0;

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;

  color: var(--ic-text-strong);
  font-size: 13px;
  line-height: 1.34;
  font-weight: 900;
}

.inc-create-selected-user-copy span,
.inc-create-target-user-copy span {
  min-inline-size: 0;

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;

  color: var(--ic-text-muted);
  font-size: 11px;
  line-height: 1.34;
  font-weight: 700;
}

.inc-create-selected-user-clear,
.inc-create-target-user-clear,
.inc-create-file-remove {
  appearance: none;

  position: relative;
  z-index: 1;

  min-block-size: 34px;
  padding-inline: 12px;

  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;

  border: 1px solid rgba(255, 255, 255, .14);
  border-radius: 17px;

  background:
    linear-gradient(180deg, rgba(255, 255, 255, .080), rgba(255, 255, 255, .032)),
    rgba(255, 255, 255, .060);

  color: var(--ic-text);

  box-shadow: var(--ic-shadow-control);

  font-size: 12px;
  line-height: 1;
  font-weight: 900;
  white-space: nowrap;

  cursor: pointer;

  transition:
    transform var(--ic-fast) var(--ic-ease),
    border-color var(--ic-fast) var(--ic-ease-standard),
    background var(--ic-fast) var(--ic-ease-standard),
    color var(--ic-fast) var(--ic-ease-standard),
    opacity var(--ic-fast) var(--ic-ease-standard);
}

.inc-create-selected-user-clear:hover,
.inc-create-target-user-clear:hover,
.inc-create-file-remove:hover {
  transform: translateY(-1px);

  border-color: rgba(255, 255, 255, .24);

  background:
    linear-gradient(180deg, rgba(255, 255, 255, .11), rgba(255, 255, 255, .042)),
    rgba(255, 255, 255, .085);

  color: #ffffff;
}

.inc-create-selected-user-clear:active,
.inc-create-target-user-clear:active,
.inc-create-file-remove:active {
  transform: translateY(0) scale(.985);
}

.inc-create-selected-user-clear:disabled,
.inc-create-target-user-clear:disabled,
.inc-create-file-remove:disabled {
  pointer-events: none;
  opacity: .52;
}


/* =========================================================
   FILES / DROPZONE
========================================================= */

.inc-create-files-head {
  position: relative;
  z-index: 1;

  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;

  min-inline-size: 0;
}

.inc-create-files-head span {
  min-block-size: 26px;

  display: inline-flex;
  align-items: center;
  justify-content: center;

  padding-inline: 10px;

  border: 1px solid rgba(255, 255, 255, .14);
  border-radius: 999px;

  background: rgba(255, 255, 255, .075);
  color: var(--ic-text-soft);

  box-shadow: var(--ic-shadow-control);

  font-size: 11px;
  line-height: 1;
  font-weight: 900;
  letter-spacing: .080em;
  text-transform: uppercase;
  white-space: nowrap;
}

.inc-create-dropzone {
  position: relative;
  z-index: 1;

  display: grid;
  align-content: center;

  min-block-size: 118px;

  padding: 16px;

  overflow: hidden;
  outline: none;

  border: 1px dashed rgba(255, 255, 255, .16);
  border-radius: 20px;

  background:
    linear-gradient(180deg, rgba(255, 255, 255, .050), rgba(255, 255, 255, .022)),
    rgba(255, 255, 255, .050);

  box-shadow: var(--ic-shadow-control);

  cursor: pointer;

  transition:
    transform var(--ic-fast) var(--ic-ease),
    background var(--ic-fast) var(--ic-ease-standard),
    border-color var(--ic-fast) var(--ic-ease-standard),
    box-shadow var(--ic-fast) var(--ic-ease-standard);
}

.inc-create-dropzone::before {
  content: "";

  position: absolute;
  inset: 0;

  pointer-events: none;

  background:
    radial-gradient(circle at 20% 0%, rgba(255, 255, 255, .10), transparent 42%);

  opacity: .75;
}

.inc-create-dropzone:hover {
  transform: translateY(-1px);
  border-color: rgba(255, 255, 255, .26);
  background:
    linear-gradient(180deg, rgba(255, 255, 255, .070), rgba(255, 255, 255, .028)),
    rgba(255, 255, 255, .065);
}

.inc-create-dropzone:active {
  transform: translateY(0) scale(.992);
}

.inc-create-dropzone:focus-within {
  border-color: rgba(125, 211, 252, .40);
  box-shadow:
    0 0 0 3px rgba(96, 165, 250, .14),
    var(--ic-shadow-control);
}

.inc-create-dropzone.is-active {
  border-color: rgba(34, 211, 238, .56);
  background:
    radial-gradient(circle at 0% 0%, rgba(34, 211, 238, .14), transparent 42%),
    rgba(34, 211, 238, .075);
}

.inc-create-dropzone.is-active .inc-create-dropzone-copy strong {
  color: #ffffff;
}

.inc-create-dropzone.is-error {
  border-color: rgba(239, 68, 68, .46);
  box-shadow:
    0 0 0 3px rgba(239, 68, 68, .13),
    var(--ic-shadow-control);
}

.inc-create-dropzone.is-error .inc-create-dropzone-copy strong {
  color: #f87171;
}

.inc-create-hidden-input {
  position: absolute;

  inline-size: 1px;
  block-size: 1px;

  overflow: hidden;

  opacity: 0;
  pointer-events: none;
}

.inc-create-dropzone-copy {
  position: relative;
  z-index: 1;

  display: grid;
  gap: 4px;

  min-inline-size: 0;
}

.inc-create-dropzone-copy strong {
  color: var(--ic-text-strong);
  font-size: 13px;
  line-height: 1.34;
  font-weight: 900;
}

.inc-create-dropzone-copy span {
  color: var(--ic-text-muted);
  font-size: 11px;
  line-height: 1.42;
  font-weight: 700;
}

.inc-create-files-list {
  position: relative;
  z-index: 1;

  display: grid;
  gap: 8px;

  max-block-size: min(280px, 38dvh);

  overflow: auto;

  padding-inline-end: 2px;

  scrollbar-width: thin;
  scrollbar-color: rgba(255, 255, 255, .22) transparent;
}

.inc-create-files-list::-webkit-scrollbar {
  inline-size: 10px;
}

.inc-create-files-list::-webkit-scrollbar-thumb {
  border: 2px solid transparent;
  border-radius: 999px;
  background: rgba(255, 255, 255, .22);
  background-clip: padding-box;
}

.inc-create-file-row {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto;
  align-items: center;
  gap: 12px;

  min-inline-size: 0;

  padding: 9px 12px;

  border: 1px solid rgba(255, 255, 255, .14);
  border-radius: 17px;

  background:
    linear-gradient(180deg, rgba(255, 255, 255, .055), rgba(255, 255, 255, .024)),
    rgba(255, 255, 255, .050);

  box-shadow: var(--ic-shadow-control);

  transition:
    border-color var(--ic-fast) var(--ic-ease-standard),
    background var(--ic-fast) var(--ic-ease-standard);
}

.inc-create-file-row:hover {
  border-color: rgba(255, 255, 255, .22);
  background:
    linear-gradient(180deg, rgba(255, 255, 255, .075), rgba(255, 255, 255, .030)),
    rgba(255, 255, 255, .070);
}

.inc-create-file-meta {
  display: grid;
  gap: 2px;

  min-inline-size: 0;
}

.inc-create-file-name {
  min-inline-size: 0;

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;

  color: var(--ic-text-strong);
  font-size: 12px;
  line-height: 1.36;
  font-weight: 900;
}

.inc-create-file-size {
  min-inline-size: 0;

  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;

  color: var(--ic-text-muted);
  font-size: 11px;
  line-height: 1.34;
  font-weight: 700;
}

.inc-create-file-remove {
  color: color-mix(in srgb, var(--ic-text) 86%, #ef4444 14%);
}

.inc-create-file-remove:hover {
  border-color: rgba(239, 68, 68, .34);
  color: #f87171;
}

.inc-create-file-remove svg {
  inline-size: 14px;
  block-size: 14px;
}


/* =========================================================
   ACTIONS / SUBMIT
========================================================= */

.inc-create-actions {
  position: relative;
  z-index: 1;

  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 8px;

  min-inline-size: 0;

  margin: 0;
  padding-block-start: 4px;

  background: transparent;
  border: 0;
}

.inc-create-submit {
  appearance: none;

  position: relative;
  isolation: isolate;

  min-block-size: 48px;
  padding-inline: 26px;

  display: inline-flex;
  align-items: center;
  justify-content: center;

  overflow: hidden;

  border: 1px solid rgba(94, 234, 212, .34);
  border-radius: 20px;

  background:
    radial-gradient(circle at 20% 0%, rgba(255, 255, 255, .15), transparent 45%),
    linear-gradient(135deg, rgba(52, 211, 153, .82), rgba(34, 211, 238, .68) 62%, rgba(96, 165, 250, .62)),
    rgba(20, 184, 166, .18);

  color: #ffffff;

  box-shadow:
    0 16px 40px rgba(34, 211, 238, .12),
    0 12px 34px rgba(52, 211, 153, .12),
    inset 0 1px 0 rgba(255, 255, 255, .14);

  font-size: 13px;
  line-height: 1;
  font-weight: 950;
  white-space: nowrap;

  cursor: pointer;

  transition:
    transform var(--ic-fast) var(--ic-ease),
    background var(--ic-fast) var(--ic-ease-standard),
    border-color var(--ic-fast) var(--ic-ease-standard),
    color var(--ic-fast) var(--ic-ease-standard),
    filter var(--ic-fast) var(--ic-ease-standard),
    opacity var(--ic-fast) var(--ic-ease-standard),
    box-shadow var(--ic-fast) var(--ic-ease-standard);
}

.inc-create-submit::before {
  content: "";

  position: absolute;
  inset: 0;
  z-index: -1;

  background:
    linear-gradient(90deg, transparent, rgba(255, 255, 255, .18), transparent);

  transform: translateX(-120%);
  transition: transform .42s var(--ic-ease);
}

.inc-create-submit:hover {
  transform: translateY(-2px);

  border-color: rgba(125, 211, 252, .46);

  background:
    radial-gradient(circle at 20% 0%, rgba(255, 255, 255, .18), transparent 45%),
    linear-gradient(135deg, rgba(52, 211, 153, .92), rgba(34, 211, 238, .78) 62%, rgba(96, 165, 250, .72)),
    rgba(20, 184, 166, .22);

  filter: brightness(1.02);

  box-shadow:
    0 18px 46px rgba(34, 211, 238, .16),
    0 12px 34px rgba(52, 211, 153, .14),
    inset 0 1px 0 rgba(255, 255, 255, .17);
}

.inc-create-submit:hover::before {
  transform: translateX(120%);
}

.inc-create-submit:active {
  transform: translateY(0) scale(.985);
}

.inc-create-submit:disabled,
.inc-create-submit[aria-busy="true"],
.inc-create-submit.is-loading {
  pointer-events: none;
  cursor: wait;
  opacity: .72;
  filter: none;
  transform: none;
}

.inc-create-submit-inner {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;

  min-inline-size: 0;
}


/* =========================================================
   FOCUS / BUSY / DISABLED
========================================================= */

.inc-create-close:focus-visible,
.inc-create-submit:focus-visible,
.inc-create-selected-user-clear:focus-visible,
.inc-create-target-user-clear:focus-visible,
.inc-create-file-remove:focus-visible,
.inc-create-user-result:focus-visible,
.inc-create-search-item:focus-visible,
.inc-create-dropzone:focus-within,
.inc-create-input:focus-visible,
.inc-create-textarea:focus-visible,
.inc-create-select:focus-visible {
  outline: none;
  box-shadow: var(--ic-focus) !important;
}

#incidencias-create-modal-root [aria-busy="true"] {
  cursor: wait;
}

#incidencias-create-modal-root button[disabled],
#incidencias-create-modal-root button[aria-disabled="true"],
#incidencias-create-modal-root input[disabled],
#incidencias-create-modal-root textarea[disabled],
#incidencias-create-modal-root select[disabled] {
  pointer-events: none;
  opacity: .62;
}


/* =========================================================
   LIGHT THEME
========================================================= */

[data-theme="light"] .inc-create-overlay {
  --ic-overlay-bg:
    radial-gradient(circle at 78% 10%, rgba(14, 165, 233, .16), transparent 34%),
    radial-gradient(circle at 16% 82%, rgba(251, 191, 36, .12), transparent 32%),
    linear-gradient(180deg, rgba(15, 23, 42, .30), rgba(15, 23, 42, .46));
}

[data-theme="light"] .inc-create-panel {
  --ic-text: rgba(15, 23, 42, .90);
  --ic-text-strong: #0f172a;
  --ic-text-soft: rgba(15, 23, 42, .80);
  --ic-text-muted: rgba(15, 23, 42, .62);
  --ic-text-dim: rgba(15, 23, 42, .45);

  --ic-panel-bg:
    radial-gradient(circle at 16% 0%, rgba(255, 255, 255, .82), transparent 34%),
    linear-gradient(180deg, rgba(255, 255, 255, .92), rgba(255, 255, 255, .70)),
    rgba(255, 255, 255, .96);

  --ic-card-bg:
    radial-gradient(circle at 12% 0%, rgba(255, 255, 255, .86), transparent 34%),
    linear-gradient(180deg, rgba(255, 255, 255, .90), rgba(255, 255, 255, .64)),
    rgba(255, 255, 255, .78);

  --ic-card-bg-hover:
    radial-gradient(circle at 12% 0%, rgba(255, 255, 255, .96), transparent 34%),
    linear-gradient(180deg, rgba(255, 255, 255, .98), rgba(255, 255, 255, .74)),
    rgba(255, 255, 255, .86);

  --ic-input-bg:
    linear-gradient(180deg, rgba(255, 255, 255, .88), rgba(255, 255, 255, .64)),
    rgba(255, 255, 255, .78);

  --ic-input-bg-hover:
    linear-gradient(180deg, rgba(255, 255, 255, .96), rgba(255, 255, 255, .72)),
    rgba(255, 255, 255, .86);

  --ic-input-bg-focus:
    linear-gradient(180deg, rgba(255, 255, 255, 1), rgba(255, 255, 255, .82)),
    rgba(255, 255, 255, .94);

  --ic-border: rgba(15, 23, 42, .10);
  --ic-border-soft: rgba(15, 23, 42, .075);
  --ic-border-strong: rgba(15, 23, 42, .18);

  color: var(--ic-text) !important;

  box-shadow:
    0 34px 92px rgba(15, 23, 42, .22),
    0 12px 34px rgba(15, 23, 42, .12),
    inset 0 1px 0 rgba(255, 255, 255, .78) !important;
}

[data-theme="light"] .inc-create-header {
  background:
    radial-gradient(circle at 0% 0%, rgba(255, 255, 255, .90), transparent 40%),
    linear-gradient(180deg, rgba(255, 255, 255, .72), rgba(255, 255, 255, .26));
}

[data-theme="light"] .inc-create-header-copy h2,
[data-theme="light"] .inc-create-alert-copy strong,
[data-theme="light"] .inc-create-block-head strong,
[data-theme="light"] .inc-create-files-head strong,
[data-theme="light"] .inc-create-dropzone-copy strong,
[data-theme="light"] .inc-create-file-name,
[data-theme="light"] .inc-create-selected-user-copy strong,
[data-theme="light"] .inc-create-target-user-copy strong,
[data-theme="light"] .inc-create-user-result-copy strong,
[data-theme="light"] .inc-create-search-item-copy strong {
  color: #0f172a !important;
  text-shadow: none !important;
}

[data-theme="light"] .inc-create-header-copy p,
[data-theme="light"] .inc-create-alert-copy span,
[data-theme="light"] .inc-create-block-head span,
[data-theme="light"] .inc-create-dropzone-copy span,
[data-theme="light"] .inc-create-file-size,
[data-theme="light"] .inc-create-selected-user-copy span,
[data-theme="light"] .inc-create-target-user-copy span,
[data-theme="light"] .inc-create-user-result-copy span,
[data-theme="light"] .inc-create-search-item-copy span {
  color: rgba(15, 23, 42, .62) !important;
}

[data-theme="light"] .inc-create-input,
[data-theme="light"] .inc-create-textarea,
[data-theme="light"] .inc-create-select {
  color: #0f172a !important;
}

[data-theme="light"] .inc-create-input::placeholder,
[data-theme="light"] .inc-create-textarea::placeholder {
  color: rgba(15, 23, 42, .42);
}

[data-theme="light"] .inc-create-close,
[data-theme="light"] .inc-create-selected-user-clear,
[data-theme="light"] .inc-create-target-user-clear,
[data-theme="light"] .inc-create-file-remove {
  color: rgba(15, 23, 42, .82);
  background:
    linear-gradient(180deg, rgba(255, 255, 255, .86), rgba(255, 255, 255, .62)),
    rgba(255, 255, 255, .74);
  border-color: rgba(15, 23, 42, .12);
}


/* =========================================================
   RESPONSIVE
========================================================= */

@media (max-width: 1120px) {
  .inc-create-overlay,
  .inc-create-panel {
    --ic-width: min(940px, calc(100vw - 32px));
    --ic-height: 94dvh;
  }

  .inc-create-overlay {
    padding: 16px !important;
  }
}

@media (max-width: 820px) {
  .inc-create-grid--2 {
    grid-template-columns: 1fr;
  }

  .inc-create-header {
    gap: 14px;
    padding: 22px 18px 18px;
  }

  .inc-create-header-copy h2 {
    font-size: clamp(34px, 9vw, 52px);
    line-height: .94;
  }

  .inc-create-body {
    padding: 18px;
  }

  .inc-create-block,
  .inc-create-files-card {
    border-radius: 22px;
  }

  .inc-create-actions {
    display: grid;
    grid-template-columns: 1fr;
  }

  .inc-create-submit {
    inline-size: 100%;
    min-block-size: 50px;
  }
}

@media (max-width: 620px) {
  .inc-create-overlay,
  .inc-create-panel {
    --ic-width: 100%;
    --ic-height: 94dvh;
  }

  .inc-create-overlay {
    place-items: end center !important;
    padding: 12px 12px 0 !important;
  }

  .inc-create-panel {
    border-radius: 26px 26px 0 0 !important;
  }

  .inc-create-header {
    grid-template-columns: minmax(0, 1fr) auto;
  }

  .inc-create-close {
    inline-size: 44px;
    block-size: 44px;
    border-radius: 17px;
  }

  .inc-create-selected-user,
  .inc-create-target-user-card {
    grid-template-columns: 1fr;
    align-items: stretch;
  }

  .inc-create-selected-user-clear,
  .inc-create-target-user-clear {
    inline-size: 100%;
  }

  .inc-create-user-result,
  .inc-create-search-item {
    grid-template-columns: auto minmax(0, 1fr);
  }

  .inc-create-user-result-copy strong,
  .inc-create-selected-user-copy strong,
  .inc-create-target-user-copy strong,
  .inc-create-user-result-copy span,
  .inc-create-selected-user-copy span,
  .inc-create-target-user-copy span {
    white-space: normal;
  }

  .inc-create-files-head {
    align-items: flex-start;
    flex-direction: column;
  }

  .inc-create-file-row {
    grid-template-columns: 1fr;
    align-items: stretch;
  }

  .inc-create-file-remove {
    inline-size: 100%;
  }
}

@media (max-width: 460px) {
  .inc-create-overlay,
  .inc-create-panel {
    --ic-width: 100vw;
    --ic-height: 96dvh;
  }

  .inc-create-overlay {
    padding-inline: 0 !important;
    padding-block-end: 0 !important;
  }

  .inc-create-panel {
    border-inline: 0 !important;
    border-block-end: 0 !important;
  }

  .inc-create-header-copy h2 {
    font-size: clamp(32px, 11vw, 44px);
  }

  .inc-create-user-result,
  .inc-create-search-item {
    grid-template-columns: 1fr;
    justify-items: start;
  }

  .inc-create-user-avatar,
  .inc-create-search-avatar,
  .inc-create-target-user-avatar {
    inline-size: 40px;
    block-size: 40px;
    min-inline-size: 40px;
    min-block-size: 40px;
  }

  .inc-create-submit {
    padding-inline: 16px;
  }
}


/* =========================================================
   FALLBACKS
========================================================= */

@supports not selector(body:has(#incidencias-create-modal-root)) {
  #incidencias-create-modal-root,
  #incidencias-detail-modal-root {
    z-index: 2147483000 !important;
  }

  .inc-create-overlay {
    z-index: 2147483001 !important;
  }

  .inc-create-panel {
    z-index: 2147483002 !important;
  }
}

@supports not ((backdrop-filter: blur(1px)) or (-webkit-backdrop-filter: blur(1px))) {
  .inc-create-overlay,
  .inc-create-loading-overlay {
    backdrop-filter: none !important;
    -webkit-backdrop-filter: none !important;
  }

  .inc-create-overlay {
    background: rgba(0, 0, 0, .86) !important;
  }

  .inc-create-loading-overlay {
    background: rgba(0, 0, 0, .72) !important;
  }
}

@supports not (color: color-mix(in srgb, #fff 50%, #000 50%)) {
  .inc-create-loading-overlay {
    background: rgba(0, 0, 0, .72);
  }

  .inc-create-input.is-error,
  .inc-create-textarea.is-error,
  .inc-create-select.is-error,
  .inc-create-dropzone.is-error {
    box-shadow: var(--ic-shadow-control);
  }
}

@media (prefers-contrast: more) {
  .inc-create-panel,
  .inc-create-block,
  .inc-create-files-card,
  .inc-create-alert,
  .inc-create-user-result,
  .inc-create-search-item,
  .inc-create-user-search-state,
  .inc-create-search-state,
  .inc-create-selected-user,
  .inc-create-target-user-card,
  .inc-create-file-row,
  .inc-create-dropzone,
  .inc-create-input,
  .inc-create-textarea,
  .inc-create-select,
  .inc-create-submit,
  .inc-create-close {
    border-width: 2px;
  }
}

@media (forced-colors: active) {
  .inc-create-panel,
  .inc-create-block,
  .inc-create-files-card,
  .inc-create-alert,
  .inc-create-user-result,
  .inc-create-search-item,
  .inc-create-user-search-state,
  .inc-create-search-state,
  .inc-create-selected-user,
  .inc-create-target-user-card,
  .inc-create-file-row,
  .inc-create-dropzone,
  .inc-create-input,
  .inc-create-textarea,
  .inc-create-select,
  .inc-create-submit,
  .inc-create-close,
  .inc-create-file-remove,
  .inc-create-selected-user-clear,
  .inc-create-target-user-clear {
    forced-color-adjust: auto;
    border-color: CanvasText;
    background: Canvas;
    color: CanvasText;
    box-shadow: none;
  }

  .inc-create-block::before,
  .inc-create-files-card::before,
  .inc-create-loading-spinner,
  .inc-create-spinner,
  .inc-create-mini-spinner {
    forced-color-adjust: none;
    border-color: Highlight;
  }
}


/* =========================================================
   PRINT
========================================================= */

@media print {
  #incidencias-create-modal-root,
  .inc-create-overlay {
    position: static !important;
    inset: auto !important;
    padding: 0 !important;

    background: transparent !important;

    backdrop-filter: none !important;
    -webkit-backdrop-filter: none !important;
  }

  .inc-create-panel {
    inline-size: 100% !important;
    max-block-size: none !important;

    overflow: visible !important;

    border: 0 !important;
    border-radius: 0 !important;

    background: #ffffff !important;
    color: #000000 !important;
    box-shadow: none !important;
  }

  .inc-create-close,
  .inc-create-actions,
  .inc-create-loading-overlay {
    display: none !important;
  }

  .inc-create-header,
  .inc-create-block,
  .inc-create-files-card,
  .inc-create-alert,
  .inc-create-user-result,
  .inc-create-search-item,
  .inc-create-file-row {
    box-shadow: none !important;
  }
}


/* =========================================================
   MOTION
========================================================= */

@keyframes incidenciasCreateOverlayIn {
  from {
    opacity: 0;
  }

  to {
    opacity: 1;
  }
}

@keyframes incidenciasCreatePanelIn {
  from {
    opacity: 0;
    transform: translateY(16px) scale(.982);
  }

  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

@keyframes incidenciasCreateSpin {
  to {
    transform: rotate(360deg);
  }
}

@media (prefers-reduced-motion: reduce) {
  .inc-create-overlay,
  .inc-create-panel,
  .inc-create-overlay *,
  .inc-create-overlay *::before,
  .inc-create-overlay *::after {
    animation: none !important;
    transition: none !important;
  }

  .inc-create-close:hover,
  .inc-create-submit:hover,
  .inc-create-user-result:hover,
  .inc-create-search-item:hover,
  .inc-create-dropzone:hover,
  .inc-create-selected-user-clear:hover,
  .inc-create-target-user-clear:hover,
  .inc-create-file-remove:hover {
    transform: none !important;
  }
}
