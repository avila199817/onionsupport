/* Generado/validado desde el árbol real del repositorio. */
export const ENTITY_STYLE_PATHS = Object.freeze({
  factura: Object.freeze([]),
  incidencia: Object.freeze([]),
  cliente: Object.freeze([]),
  usuario: Object.freeze([]),
});

export default ENTITY_STYLE_PATHS;
